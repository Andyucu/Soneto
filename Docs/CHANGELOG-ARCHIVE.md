# Changelog

All notable progress on Soneto is logged here, newest first. Format is free-text per entry — this is a build log, not a semver changelog (no releases yet).

## 2026-09-03 (Phase 3 item 11 — docs half; the demo itself is an open, human-only gap)

- **Phase 3 item 11's spec is two things: an `Docs/ARCHITECTURE.md` update, and a real human running one continuous end-to-end walkthrough.** Only the first is closeable from an agent session — the walkthrough needs a real hotkey press plus real speech into a real microphone, which this project's standing live-desktop-testing caution correctly rules out.
- **`Docs/ARCHITECTURE.md`** retitled ("as-built, Phase 1" → "as-built, Phases 1-3") and gained three new sections: the full §3.3 "why two executables" reasoning (the rejected IPC-between-daemon-and-UI alternative, and why `Soneto.Daemon` was deliberately kept rather than deleted once `Soneto.App` became the real product); "The dictionary engine (Phase 2)" — previously entirely undocumented in this file — covering the five entry types, the four post-processor stages (orders 40-70), the seed dictionary, and the standing "no live rebuild into a running session" limitation Phase 3's Dictionary editor inherits and surfaces honestly; "The Avalonia shell (Phase 3)" — the composition-root "eagerize a service out of the background pipeline" pattern items 6/7/8 each independently arrived at, `DictationCompleted` as the sole pipeline→UI bridge, History/SQLite's architecture, item 10's data & privacy mechanisms, and the injectable-ViewModel unit-testing pattern used throughout `Soneto.App.Tests`.
- **`Docs/MANUAL-TESTS.md`** gained a new Section C.1 — a real, checkbox-per-step continuous walkthrough script matching item 11's own literal done-when bar exactly (launch → dictate exercising a real dictionary rule → confirm a correct History diff → edit that rule in the Dictionary editor → confirm the "changes apply on next restart" notice is actually true, not just displayed → confirm Settings/Permissions Doctor both reflect real live state → restart and confirm the edit took effect → exercise item 10's debug-audio-retention/auto-delete/panic-wipe controls in the same sitting) — so a human has a concrete, executable script rather than an unstructured verbal instruction to work from.
- **The actual walkthrough has not been run by anyone as of this writing.** This mirrors Phase 1 items 11/12's own honest structural-gate precedent exactly: real, reusable infrastructure (the updated architecture doc + the new checklist) is built and ready; the human-only step is named explicitly as open, not silently skipped or falsely claimed done.
- **Phase 3's main build order (items 0-11) is now code/docs-complete**, with this item's own human walkthrough as the one remaining gate before the phase can be considered fully closed.

## 2026-09-03 (Phase 3 item 10)

- **Phase 3 item 10 (Data & privacy controls), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 10 done.** Opt-in debug-audio retention (off by default), a daily history auto-delete sweep, and a panic-wipe button gated behind a real two-click confirmation dialog.
- **`DataPrivacyConfig`** (new, `SonetoConfig.cs`): `DebugAudioRetentionEnabled` (default `false`), `DebugAudioRetentionMaxClips`, `HistoryAutoDeleteAfterDays` (default `null` = never). An old `config.json` with no `dataPrivacy` key deserializes safely with these defaults — verified live against this machine's real, pre-item-10 `config.json`.
- **Audio-correlation mechanism — the one deliberate touch to `SessionController.cs` this item makes:** `DictationCompletedEventArgs` gained `AudioSamples` (`ReadOnlyMemory<float>`), a zero-copy slice threaded mechanically from `RunFinalizingAsync`'s existing VAD-trimmed `trim.TrimmedSamples` through `RunTranscribingAsync`/`RunInjectingAsync`/`RaiseDictationCompleted` — same threading discipline as item 2. `App.axaml.cs`'s `AppendHistoryEntryAndMaybeSaveDebugAudioSafelyAsync` writes the WAV (`{historyId}.wav`, via `DebugAudioStore`) only AFTER `historyStore.AppendAsync` returns its real rowid, so the correlation filename can never use a stale/wrong Id.
- **Two genuinely independent purge policies, per §3.14's explicit requirement:** `DebugAudioStore` purges by count ("keep last N clips", oldest-first) immediately after each write; a new `HistoryRetentionSweeper` purges history text by age via a daily `Timer` calling the already-tested `IHistoryStore.PurgeOlderThanAsync`. The sweeper deliberately has no generation-token guard (unlike this project's 8+ prior hardened timers) — verified correct by tracing, not assumed: a stale/late tick only re-runs an idempotent DB delete, since (unlike WarmIdle's stream or the hotkey heartbeat) there is no live resource a superseded tick could clobber.
- **Panic wipe** requires two genuinely separate actions across two distinct controls (a trigger button, then a separate `ConfirmDialog` modal's own Confirm button — no `IsDefault`/`IsCancel` on either, so no accidental Enter-key or double-click path exists) and empties both the history store (`PanicWipeAsync`) and the debug-audio clip directory.
- **Independent test-runner verification** traced the audio buffer lifecycle by hand (`PortAudioCapture.EndCapture`'s `ToArray()` produces a fresh, un-pooled array — no aliasing/reuse hazard reaches the async WAV write) and confirmed all of the above by direct code tracing, not just trusting doc comments. It also surfaced a real, reproducible test-infrastructure flake: `SettingsViewModelTests`' 150ms wall-clock settle-timeout raced `SaveAsync`'s async continuation under full-solution parallel test load (~1-in-3 to 1-in-6 full-solution runs failed a different `Changing*Fields_WritesCorrectlyShapedConfig` test each time).
- **Code review confirmed no blocking issues in production code** (all five specifically-flagged risks — timer lifecycle, audio aliasing, WAV-write failure handling, config defaults, confirmation-dialog safety — independently re-verified true, not just accepted) and diagnosed the flake's exact root cause (a subscribe-after-file-write race, not a `SaveAsync` bug), recommending a deterministic TCS-gate fix.
- **Flake fixed**: `FakeConfigService`/`FakeDictionaryService` gained a `WaitForNextSubscriptionAsync()` TCS-gate (mirroring `FakeHistoryStore`'s existing `AddGate`/`ReleaseGate` idiom from item 6) so tests await real event-handler subscription before firing a simulated reload, instead of racing a wall-clock poll. Applied to all 8 affected `SettingsViewModelTests` and, proactively, to the identical latent (not-yet-observed) race found in `DictionaryEditorViewModelTests`. Confirmed fixed across 6 consecutive full-solution `dotnet test soneto.slnx` runs, all clean.
- `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs. `dotnet test soneto.slnx` (full solution, 6 consecutive runs post-fix, all identical/clean): `Soneto.Core.Tests` 481/481 (up from 467), `Soneto.Platform.Windows.Tests` 100/102 (2 pre-existing hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 55/55 (unchanged), `Soneto.App.Tests` 60/60 (up from 47).
- **Actually ran `Soneto.App.exe`** with the real local model: startup log showed the new eager `HistoryRetentionSweeper` construction/start executing silently and cleanly alongside the existing pipeline-startup sequence, no crash, no meaningful delay, config with no `dataPrivacy` section loading correctly with safe defaults.
- **Honest gaps, same style as items 3-9's:** no screenshot tooling available to the implementer/test-runner sessions, so the actual rendered appearance of the new Settings "Data & privacy" section and the `ConfirmDialog` modal was not visually confirmed — needs the orchestrating session's/a human's own follow-up pass. A real end-to-end "enable debug-audio-retention → dictate → confirm a correlated WAV lands" walkthrough was not performed live (would require real speech into the mic); the write/purge/correlation logic is instead proven directly against real files by `DebugAudioStoreTests`. The panic-wipe dialog's actual on-screen click-through was not manually driven (avoided per the standing live-desktop-testing caution — see [[soneto-live-desktop-testing-caution]]); both should be human/orchestrator-verified before fully trusting this item's UI path.
- **Incident note, no harm done:** during the implementer's live run, an unexpected real global RightControl press occurred on the shared desktop (not sent by any agent) and briefly opened a real mic stream; killed within ~2s, and confirmed afterward that no debug-audio file was written, `history.db` was untouched, and `config.json` was unchanged — consistent with this project's standing live-desktop-testing caution, not a new failure mode.

## 2026-09-03 (Phase 3 item 9 — orchestrating-session verification + code review)

- **Visual follow-up:** launched `Soneto.App.exe` live, navigated to the Permissions page via safe `SendInput` mouse clicks (keyboard synthetic input remains correctly blocked by this environment's own permission classifier — respected, not worked around). Real screenshots (reviewed then deleted immediately, since they capture the whole desktop) confirmed all five checks genuinely go Green with real diagnostic text; a genuine Windows "Microphone accessed" system notification appeared during the mic check; the Model files check was caught mid-computation ("CHECKING…") in one screenshot before completing Green in a follow-up shot, confirming the hash computation is genuinely asynchronous.
- **Code review: no blocking issues.** Independently re-confirmed both safety-critical constraints hold: the only two `WindowsHotkeySource`/`LinuxHotkeySource` construction sites in the repo remain inside `DaemonComposition.CreatePlatformHotkeySourceAndTextInjector`, called once each by `Soneto.Daemon`/`PipelineHost` — the Permissions Doctor never builds its own; the injection self-test has no `await`/yield point between `Focus()` and `CaptureTarget()`, so there is no window for OS focus to drift elsewhere; `WindowsTextInjector`/`LinuxTextInjector` aren't `IDisposable`, so the throwaway self-test injector leaks nothing. Also confirmed `ResolveModelDirForCheck` mirrors `BuildAndStartSessionControllerAsync`'s real resolution order line-by-line, and that Windows/non-Windows clipboard gating doesn't cross-leak.
- **Four should-fix items applied, re-verified with a full rebuild + two-TFM test run afterward (467/100-102/55/47, unchanged, 0 warnings/errors):**
  - `PipelineHost.LastStarted`/`LastFailureReason` were plain auto-properties with no cross-thread visibility/ordering guarantee, despite being documented as a late-subscriber safety net for a reader on a different thread — now guarded by a private lock (`src/Soneto.App/PipelineHost.cs`).
  - `PermissionCheckViewModel.IsPending` existed but was never actually bound to the Warning style class, contradicting the view's own doc comment claiming "NotTested and Pending both use the Warning token" (a check mid-run rendered with the default Body color, not Warning) — added a computed `IsWarn` property covering both `NotTested` and `Pending`, rebound `Classes.CheckWarn` to it (`PermissionCheckViewModel.cs`, `PermissionsDoctorView.axaml`).
  - `PermissionsDoctorViewModel`'s own doc comment still described the abandoned `Opacity="0"`/1x1px self-test-TextBox approach, directly contradicting the axaml's own (correct) comment about the negative-margin fix for that same historical bug — corrected to match.
  - A real but currently-unreachable latent gap was surfaced and **deliberately left unfixed**, per this project's "don't add code for scenarios that can't happen" discipline: `SessionController` has no terminal "torn down" state, so if a future item ever disposes a running controller at runtime (nothing does today — "Pause dictation" is inert), `EvaluateHookCheck` would keep reporting a stale Green rather than recognizing the pipeline is gone. Flagged here and in `Docs/PROJECT-MEMORY.md` as a known gap to close if/when pause/restart wiring is ever built.
- **Item 9 is now fully closed out** — implementation, live verification, visual confirmation, and code review all complete.

## 2026-09-03 (Phase 3 item 9)

- **Phase 3 item 9 (Permissions Doctor), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 9 done.**
- **`PermissionsDoctorViewModel`/`PermissionCheckViewModel`/`PermissionsDoctorLogic`** (new, `src/Soneto.App/ViewModels/`) plus a real `PermissionsDoctorView.axaml` replace item 4's placeholder. Five real checks (Mic access, Global hook active, Can synthesize input, Clipboard read/write, Model files present & hashed), each running a genuine test against real environment state, each independently re-runnable (a "Recheck all" button; per-check `RecheckAsync` also exposed).
- **Hook-collision-avoidance (constraint 1) — never a second `IHotkeySource`.** `PipelineHost.PipelineStartedEventArgs` widened (small, additive) to also expose the REAL, already-started `IHotkeySource` `PipelineHost.StartAsync` already builds; the "global hook active" check subscribes to that SAME instance's `Faulted` event plus the shared `SessionController.StateChanged`/`State`, never constructing its own — respecting SharpHook's documented single-hook-instance limitation on this machine (Phase 1 item 6's finding) by construction, not convention. A new `PipelineHost.Failed` event (+ `LastStarted`/`LastFailureReason` static late-subscriber safety nets) lets the check report an honest reason when the pipeline never starts. `PermissionsDoctorLogic.EvaluateHookCheck` (pure, independently unit-tested) resolves to Green/Red/**NotTested** — a real, distinct third state (not a false red) when `PipelineHost.Started` never fired this session, with the real failure reason surfaced alongside when known.
- **Injection self-test (constraint 2) — confirmed to never touch a window outside `Soneto.App`.** A real, normally-rendered `TextBox` lives inside the Permissions Doctor's own visual tree, moved far outside the window's visible area via a large negative `Margin` (a real bug found this needed to be a genuinely-rendered element, not `Opacity="0"` — see below). `RunInjectionSelfTestAsync` focuses it via ordinary Avalonia `Focus()` (never `GetForegroundWindow()`/anything global), then immediately calls a FRESH, throwaway `ITextInjector`'s `CaptureTarget()` — safe specifically because `Focus()` just gave OUR OWN control real OS focus, so the captured target is this process's own window, never an arbitrary foreground app — then `InjectAsync`s a marker string and verifies it landed in the TextBox. A fresh injector instance is used (never the real pipeline's own), safe because `WindowsTextInjector`/`LinuxTextInjector` are one-shot clipboard+paste operations with no persistent hook, unlike `IHotkeySource`.
- **Clipboard mechanism chosen and documented:** `Soneto.Platform.Windows.ClipboardManager` directly on Windows (the exact raw Win32 mechanism the real `WindowsTextInjector` uses — more accurate than Avalonia's own higher-level abstraction, which the real injector never touches); Avalonia's cross-platform `IClipboard` (via a lazily-resolved accessor the view supplies from its own `TopLevel`) on non-Windows, since re-deriving `Soneto.Platform.Linux`'s own wl-copy/xclip backend-selection logic would risk drifting from `LinuxTextInjector`'s real implementation for no benefit this lightweight round-trip check needs.
- **Model files present & hashed:** reuses `ModelManager.AreRequiredFilesPresent`/`MissingFiles`/`ComputeSha256Async`/`ResolveStandardModelsBaseDir` and `DaemonComposition.FindDevModelDirWalkingUp` directly (zero new hashing logic) via a new pure `PermissionsDoctorLogic.ResolveModelDirForCheck` helper mirroring `BuildAndStartSessionControllerAsync`'s own resolution order (config override → repo-local dev dir → standard location) but never downloading anything (a read-only diagnostic).
- **Linux best-effort checks** (`/dev/uinput` writable, `ydotoold` socket existence — mirroring `LinuxTextInjector`'s own existing probe exactly), shown only on Linux, no remediation UX per §3.1/§3.13's own scope note — honestly flagged as unverified on real Linux hardware from this Windows-only session, matching Phase 1 items 11/12's established precedent.
- **Real bug #1, found and fixed via this item's own live verification:** `MainWindow` eagerly constructs all four nav pages up front (item 4's own "built once and cached" shell), so an initial design running every check inside `PermissionsDoctorViewModel`'s own constructor fired long before a user ever clicked the "Permissions" tab — the self-test's `Focus()` call was a silent no-op against a `TextBox` not yet attached to any window. Fixed by deferring the first run to a new `RunInitialChecksIfNeededAsync`, called from `PermissionsDoctorView`'s own `Loaded` event (idempotent, so a later re-visit to the tab doesn't silently re-trigger a real mic-open/synthetic-paste/clipboard round trip the user didn't ask for — "Recheck all" remains the explicit way to intentionally re-run everything).
- **Real bug #2, found and fixed via this item's own live verification:** the injection self-test's first attempt used `TimeSpan.Zero` for both `InjectionOptions.PreDelay`/`ClipboardRestoreDelay` (trying to make the test as fast as possible) — reproduced live as a deterministic failure (`outcome=Injected` but `markerLanded=false`): since the self-test's target lives in Soneto.App's own UI thread (unlike a real dictation's separate target process), the clipboard-restore step reliably won the race against Avalonia's own asynchronous Ctrl+V paste handler, wiping the marker off the clipboard before the TextBox ever read it. Fixed by using the same real default delays production dictation uses (`PreDelayMs=20`/`ClipboardRestoreDelayMs=150`) plus a short settle wait before reading `TextBox.Text` back. Also independently rediscovered the `--` inside an XML comment breaking `PermissionsDoctorView.axaml`'s parsing (`AVLN1001`) — the same bug class item 7's own history already hit once; fixed the same way (remove the double-hyphen).
- **Deliberately-broken-check verification actually performed (this item's own explicit "done when" bar), via a temporary `--debug-verify-permissions-doctor` CLI-gated harness** (mirroring item 5's `--debug-hud-harness` precedent exactly, deleted immediately after this one verification run, confirmed by a clean post-removal rebuild): with the real local model present, all five checks reported Green on a real run; `config.json`'s `asr.modelDir` was then pointed at a real nonexistent path, and a second real run showed the Model files check correctly reporting **Red** with the exact real `ModelFilesMissingException` message — and, as a bonus confirmation of constraint 1's own design, the real pipeline itself failed to start under this same broken config, with the Hook check correctly reporting **NotTested** (not a false red) and the real failure reason surfaced alongside it verbatim, while Mic/Injection/Clipboard stayed Green throughout, proving those three checks genuinely work independent of whether the real pipeline is up. `config.json` was restored afterward and a third real run confirmed all five checks return to Green.
- **12 new `PermissionsDoctorLogicTests`** covering `EvaluateHookCheck`'s full red/green/not-tested state machine across every `PipelineHost.Started`-fired-or-not scenario, and `ResolveModelDirForCheck`'s full config-override/dev-dir/standard-dir/not-found resolution order — pure, fake-delegate-driven per §3.15's "extract the pure parts" instruction; the real I/O-touching checks themselves are deliberately not force-fit into fakes, verified live instead.
- `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs. `dotnet test soneto.slnx` run twice, full solution, both stable: 467 `Soneto.Core.Tests` (unchanged), 100/102 Windows (2 pre-existing hardware-gated skips, unchanged), 55 Linux (unchanged), 47 `Soneto.App.Tests` (up from 35).
- **Actually ran `Soneto.App.exe`** repeatedly and confirmed via redirected console log output that it starts cleanly with zero exceptions and the real pipeline still starts successfully end-to-end (unaffected by this item's changes); via the temporary debug harness, confirmed all five checks genuinely execute and report real, physically-verified results read directly from log output. **Honest gap, same style as prior items:** the actual on-screen rendering of the Permissions page (status colors/layout, card styling, button enabled/disabled state) was not visually confirmed by this implementer session (no screenshot tooling available here) — needs the orchestrating session's/a human's own follow-up pass, per items 3-8's established precedent.
- **Phase 3 item 9 is done**, with the hook-collision-avoidance design, the own-window-only injection self-test design, the clipboard mechanism choice, and the real deliberately-broken-check verification all documented in full here and in `Docs/soneto-implementation-plan-phase3.md` §3.16's item 9 row and `Docs/PROJECT-MEMORY.md`'s Phase 3 section.

## 2026-09-02 (Phase 3 item 8)

- **Phase 3 item 8 (Settings page), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 8 done.**
- **User-approved scope decision, deliberately narrower than §3.12's literal text — documented explicitly, not a silent shortcut.** §3.12 describes a second-hotkey "language profile hint" capture that tags a completed dictation/history entry. This item builds ONLY the capture mechanism (a new `LanguageProfileConfig.SecondaryTriggerKey` config field + a real Settings UI control, clearly labeled "not yet active — captured for a future phase") and does NOT wire it into a live second global hotkey, and does NOT add a hint field to `DictationCompletedEventArgs`/`HistoryEntry`. Reasoning: Phase 1's own history (`Docs/PROJECT-MEMORY.md`) found and reproduced TWICE that SharpHook (the hook library `WindowsHotkeySource` wraps) cannot run two concurrent hook instances in one process on this machine — a genuinely live second hotkey would require either two simultaneous hook instances (broken here) or extending `IHotkeySource`/`WindowsHotkeySource`/`LinuxHotkeySource` (already hardened through multiple Phase 1 review rounds) to recognize a second trigger key within ONE hook, real platform-code work well beyond "Settings page UI work." The user was asked directly and chose this conservative option. `SessionController`/`WindowsHotkeySource`/`LinuxHotkeySource`/`DictationCompletedEventArgs`/`HistoryEntry` were NOT touched at all this item.
- **Architecture decision: `IConfigService` decoupled from `PipelineHost`'s success**, mirroring item 7's `IDictionaryService` decoupling (which itself mirrored item 6's `IHistoryStore` decoupling). Construction+load+watch moved out of `PipelineHost` (which previously built its own `IConfigService` internally) and into `App.axaml.cs`'s eager composition root, alongside the already-eager `SqliteHistoryStore`/`IDictionaryService`. `PipelineHost.StartInBackground` now takes both the already-loaded `IConfigService` and `IDictionaryService` as parameters. Confirmed `Soneto.Daemon` is completely unaffected: it never calls `PipelineHost` (that class is `Soneto.App`-only) and continues constructing/loading/watching its own `IConfigService` via `DaemonComposition` directly, unchanged.
- **The exact same deadlock-safe blocking-load pattern item 7 already built and fixed for a real deadlock was reused verbatim, not rediscovered:** `Task.Run(() => DaemonComposition.LoadAndStartWatchingConfigAsync(configService, configLogger)).GetAwaiter().GetResult()` in `App.axaml.cs`, called BEFORE `MainWindow` is constructed. config.json's read+validate is exactly as fast/non-model-loading as dictionary.json's, so item 7's own "brief, deliberate, documented synchronous wait, never on the ASR model load" reasoning applies unchanged — see that item's own doc comment (still in `App.axaml.cs`) for the full deadlock story this reuses rather than rediscovers.
- **New public `ConfigJsonOptions` class** (`src/Soneto.Core/Configuration/ConfigJsonOptions.cs`), extracted out of `ConfigService`'s former private `BuildOptions` method (mirroring `DictionaryJsonOptions`'s existing precedent), so `SettingsViewModel` — a second writer of `config.json` — reuses the EXACT same `JsonSerializerOptions` (camelCase naming, case-insensitive reads, the soft-fallback `CaptureMode` converter) `ConfigService` itself reads with, rather than risking drift. `ConfigService.BuildOptions` now just supplies its own logger-backed fallback callback to the shared factory — zero behavior change to `ConfigService` itself, confirmed by the full unchanged `Soneto.Core.Tests` suite.
- **New `LanguageProfileConfig` section on `SonetoConfig`** (`SecondaryTriggerKey`, nullable string, same schema-string shape as `HotkeyConfig.Key`), following `SonetoConfig.cs`'s exact existing nested-config-class pattern, with a doc comment documenting the full scope decision above.
- **A small, genuinely reusable `KeyCaptureField` control** (`src/Soneto.App/Controls/KeyCaptureField.axaml`/`.cs`) — an ordinary, in-window `Button` (a `StyledProperty<string?> CapturedKey`, two-way bindable) that enters a "capturing" state on click, focuses itself, and captures the NEXT `KeyDown` it itself receives as the new value. This is safe, ordinary focused-control keyboard input, NOT a global `SharpHook`/`IHotkeySource` registration — built once and reused verbatim for both the primary Hotkey rebind field and the second-hotkey language-profile-hint field, per this item's own "build it once" instruction.
- **`SettingsViewModel`** (new, `src/Soneto.App/ViewModels/SettingsViewModel.cs`) mirrors `DictionaryEditorViewModel`'s hand-rolled `INotifyPropertyChanged`/injectable-`uiThreadPost`/settle-timeout pattern. Reads `Current` synchronously at construction (Hotkey, ASR, Audio/VAD, Injection, and the new Language profile section). **Save UX decision, documented in the class's own doc comment:** ONE "Save settings" action for the whole page, not per-field live-write or a per-section Save button — `config.json` has no per-entry validation/rejection concept the way `dictionary.json` does (a bad value hard-fails the WHOLE file's deserialization, silently retaining the previous config), so one atomic round trip is simplest and avoids a "did section A's Save silently drop my unsaved edit to section B" surprise; fields the page does not expose (`Injection.PerApp`, `Logging`, several `Audio`/`Asr` sub-fields) are carried through verbatim from `Current` at save time, never reset to defaults.
- **Scope lines drawn and documented, mirroring item 7's precedent:** `Audio.DeviceId` is a plain text field, not a real device picker (would need querying `PortAudioCapture`'s device enumeration — more than this item needs); `Asr.DecodingMethod` is a plain text field (no fixed dropdown — `SherpaOnnxTranscriber` accepts an open string). Numeric fields (`Asr.NumThreads`, `Vad.Threshold`/`MinSilenceMs`/`MinSpeechMs`) are plain `TextBox`es bound to string properties with explicit parse-and-validate-before-write logic (a parse failure aborts the save with an error status, writes nothing). Enum fields (`Audio.CaptureMode`, `Injection.Method`, `Injection.ClipboardPolicy`) are `ComboBox`es bound to `Enum.GetNames`-sourced string lists exposed as INSTANCE properties, not `static` — a real Avalonia compiled-binding build error was hit and fixed here (`AvaloniaUseCompiledBindingsByDefault=true` cannot resolve static members of the bound `x:DataType`, even though the values never vary per instance).
- **Write path mirrors item 7's dictionary editor exactly (ZERO new watch-side code):** build the full `SonetoConfig`, serialize via `ConfigJsonOptions.Create()`, `File.WriteAllTextAsync` straight to `IConfigService.ConfigPath` — the already-running `ConfigService.StartWatching()` (started once, eagerly, in `App.axaml.cs`) picks it up via its existing 500ms-debounced watcher; after writing, waits for the next `ConfigChanged` event or a 1.5s settle timeout to report success/failure.
- **`SettingsView.axaml`** replaces the item-4 placeholder: five sectioned `Border` cards (Hotkey, ASR, Audio, Injection, Language profile binding) plus a single "Save settings" button at the bottom. Every color/spacing/dimension value is a `{DynamicResource ...}` token binding, matching every prior view's discipline; no new sizing tokens were needed. The Language profile binding section's inert status is shown as real, visible UI copy ("Not yet active — captured for a future phase..."), mirroring item 7's `PerAppOverride` "not yet active" labeling precedent.
- **`FakeConfigService`** (new, `tests/Soneto.App.Tests/FakeConfigService.cs`, mirroring `FakeDictionaryService`'s real-temp-file-backed pattern exactly) points `ConfigPath` at a genuine throwaway temp file and exposes `SimulateSuccessfulReload`/`SimulateReloadFailed`/`SimulateExternalChange` so a test can drive all the real outcomes deterministically.
- 9 new `SettingsViewModelTests`: construction reads every exposed field correctly from a populated config; changing Hotkey/ASR/Audio/Injection fields each produces a correctly-shaped write (verified against the real written+reloaded `Current`); changing the language-profile secondary-trigger key writes to the new `LanguageProfile` section correctly; a save preserves fields this page doesn't expose (`Injection.PreDelayMs`/`ClipboardRestoreDelayMs`/`SanitizeModifiers`/`TargetLostPolicy`, `Logging`); an invalid numeric field aborts the save with an error status and writes nothing; an externally-triggered `ConfigChanged` (a "hand-edit," not the ViewModel's own write) triggers a live refresh marshaled through the injected `uiThreadPost` — the same test-facing-constructor shape `HistoryViewModel`/`DictionaryEditorViewModel` already established.
- `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs. `dotnet test soneto.slnx`, run twice for stability (no flake observed either time): 467/467 `Soneto.Core.Tests` (unchanged — only the additive `LanguageProfileConfig` section and the new public `ConfigJsonOptions` class touched `Soneto.Core`, both non-breaking), 99 Windows (2 pre-existing skips)/52 Linux unchanged, `Soneto.App.Tests` 34/34 (25 + 9 new).
- **Actually ran `Soneto.App.exe`** with the real local model: the log showed the config load now happening synchronously before `MainWindow`/`PipelineHost` (`"Config loaded from ... captureMode=OnDemand numThreads=4 hotkey=RightControl"` right after the dictionary-load lines, before `PipelineHost`'s "starting real pipeline composition" line), followed by the full expected pipeline-startup sequence through to "real pipeline started successfully," process alive (~928MB working set, consistent with a loaded ASR model) with zero exceptions — confirming the refactored `PipelineHost.StartInBackground(loggerFactory, configService, dictionaryService)` call site works end to end. Terminated cleanly via `taskkill`.
- **Honest gap, same style as items 3-7's:** no screenshot/visual-inspection tooling available to this implementer session, so the Settings page's actual rendered appearance (section layout, the `KeyCaptureField` control's click-to-capture visual behavior, the ComboBox dropdowns) was NOT visually confirmed by the agent — needs the orchestrating session's/a human's own follow-up pass. No real global-hotkey-press verification was attempted (correctly out of scope — this item builds no live second hotkey).
- **Addendum — the orchestrating session's own visual follow-up confirmed the page renders and reflects real config values**, via a real screenshot after a safe, targeted mouse click on the Settings nav item: Hotkey "RightControl" with Suppress checked, ASR thread count "4", decoding method "greedy_search", Audio capture mode "OnDemand" — all matching exactly what the same session's startup log showed being loaded from the real `config.json` moments earlier.
- **Code review then found TWO BLOCKING bugs, both closed same day.** (1) **`KeyCaptureField` produced key strings the platform hotkey mappers couldn't parse for the Windows/context-menu keys.** It read Avalonia's own `Key` enum name verbatim via `.ToString()` (`"LWin"`/`"RWin"`/`"Apps"`), while `HotkeyKeyMapper`/`EvdevKeyMapper`'s alias tables only registered `"LeftWin"`/`"RightWin"`/`"ContextMenu"` — a user capturing the Windows key or context-menu key as their LIVE primary hotkey via Settings would have saved a value that throws `ArgumentException` the next time `SessionController` builds a real `HotkeyBinding` from it, silently breaking dictation after a Settings save that reported success with no error shown. This was a real live-path bug, not the deliberately-deferred secondary-trigger feature. Fixed by adding `"LWin"`/`"RWin"`/`"Apps"` as additional aliases to BOTH `HotkeyKeyMapper` (Windows) and `EvdevKeyMapper` (Linux), mirroring the existing `"RightCtrl"`-alongside-`"RightControl"` compatibility-bridge pattern already established in both tables. New test coverage added to `HotkeyKeyMapperTests`/`EvdevKeyMapperTests`. (2) **`SettingsViewModel.RefreshFromCurrent` could silently clobber an in-progress edit to a DIFFERENT field during a pending save's settle window.** Unlike `DictionaryEditorViewModel` (whose list state and edit-buffer fields are genuinely separate), this page's single "Save the whole page" design means `RefreshFromCurrent` writes directly into the SAME bound properties every input control edits — only the Save button, not the rest of the form, was disabled during the ~500ms-1.5s settle wait, so an edit to a different field during that window would be silently overwritten by the stale pre-save value once the settle-triggered refresh ran. Fixed by disabling the ENTIRE form (not just Save) while `IsSaving` is true, eliminating the race by construction rather than per-field dirty-tracking — appropriate for this single-page/single-save-action model. New test `IsSaving_StaysTrueForTheEntireWriteThenSettleWindow_AndFalseOnlyAfter` proves the property the UI fix depends on.
- **The user-approved scope boundary was independently confirmed honored by code review**: zero references to the deferred secondary-trigger feature in `SessionController.cs`/`WindowsHotkeySource.cs`/`LinuxHotkeySource.cs`, and `DictationCompletedEventArgs`/`HistoryEntry` gained no new field — file mtimes independently corroborated this. `ConfigJsonOptions`'s extraction from `ConfigService` (already-hardened Phase 1 code) confirmed behavior-preserving, including the documented `CaptureModeJsonConverter` asymmetric-fallback ordering. `PipelineHost`'s refactored 3-parameter signature confirmed correctly ordered at its one call site, and `Soneto.Daemon`'s isolation reconfirmed (zero references).
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 467/467 (unaffected), `Soneto.Platform.Windows.Tests` 100/102 (2 pre-existing hardware-gated skips, +3 new alias tests), `Soneto.Platform.Linux.Tests` 55/55 (+3 new), `Soneto.App.Tests` 35/35 (34 + 1 new). Re-verified with a final live run reaching the same successful pipeline-startup sequence.
- **Phase 3 item 8 is done**, with genuine visual confirmation, two real blocking bugs caught and fixed post-review, the `IConfigService`/`PipelineHost` decoupling verified via a real live run, the second-hotkey capture mechanism built and clearly labeled inert, and the deliberate, user-approved scope reduction from §3.12's literal text documented in full here and in `Docs/soneto-implementation-plan-phase3.md` §3.16's item 8 row and `Docs/PROJECT-MEMORY.md`'s Phase 3 section.

## 2026-09-02 (Phase 3 item 7)

- **Phase 3 item 7 (Dictionary editor), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 7 done.**
- **Architecture decision: `IDictionaryService` decoupled from `PipelineHost`'s success**, mirroring item 6's `IHistoryStore` decoupling. Construction+load+watch moved out of `PipelineHost` (which previously built its own `IDictionaryService` internally) and into `App.axaml.cs`'s eager composition root, alongside the already-eager `SqliteHistoryStore`. `PipelineHost.StartInBackground` now takes the already-constructed, already-loaded `IDictionaryService` as a parameter so the real post-processor chain still gets built from real dictionary entries if/when the ASR model loads. Confirmed `Soneto.Daemon` is completely unaffected: it never calls `PipelineHost` at all (that class is `Soneto.App`-only).
- **"Block briefly on the dictionary load, never on the ASR model load" reasoning, and a real deadlock bug caught fixing it.** `DictionaryService.LoadAsync()` does real, if fast, work (file read, JSON validation, possible first-run seed write) that the Dictionary editor needs already completed by construction time; unlike `SqliteHistoryStore`'s genuinely-lazy eager construction, this needed an actual synchronous wait — judged acceptable since dictionary.json's read+validate is nothing like the ASR model's ~1.7-2s cold load (why `PipelineHost` must stay fire-and-forget). Mechanism: `Task.Run(...).GetAwaiter().GetResult()` inside `OnFrameworkInitializationCompleted`. The FIRST implementation used a bare `.GetAwaiter().GetResult()`, reasoned (wrongly) as safe because the awaited code never touches Avalonia APIs directly — a real live run proved this deadlocks: Avalonia has already installed its own `SynchronizationContext` on the UI thread by the time `OnFrameworkInitializationCompleted` runs, so every plain `await` inside `LoadAsync` captures and tries to resume on that same, now-blocked, thread. Confirmed concretely (app hung indefinitely: no window/tray, flat ~98MB memory, zero log output, for 30+ seconds) before being fixed with `Task.Run(...)` first (a thread-pool thread has no captured context to deadlock on). Re-verified live after the fix: `Soneto.App.exe` reached ~902MB working set (ASR model loaded) within ~6s with the full expected pipeline-startup log sequence (dictionary loaded+watched, config loaded+watched, ASR cold load, VAD load, hotkey started, "real pipeline started successfully").
- **`DictionaryEditorViewModel`** (new, `src/Soneto.App/ViewModels/DictionaryEditorViewModel.cs`) mirrors `HistoryViewModel`'s hand-rolled `INotifyPropertyChanged`/injectable-`uiThreadPost` pattern, plus an injectable settle timeout for tests. Reads `Current.Entries`/`RejectedEntries` synchronously at construction, exposes a `DictionaryEntryKind?` type filter over the 5 fixed entry types, per-row enable/disable, and add/edit/delete for all 5 types (`PerAppOverride` labeled "not yet active" in the UI).
- **Write path (ZERO new watch-side code, per this item's own requirement):** construct the appropriately-typed `DictionaryEntry` (new `Guid.NewGuid()`-based Id for new entries), merge/remove it from a copy of `IDictionaryService.Current.Entries`, serialize as a `DictionaryDocument` via `DictionaryJsonOptions.Create()`, `File.WriteAllTextAsync` straight to `IDictionaryService.DictionaryPath` — no new `FileSystemWatcher`/timer; the already-running `DictionaryService.StartWatching()` (started once, eagerly, in `App.axaml.cs`) picks the write up via its existing 500ms-debounced watcher. Verified for real via a safe scratch PowerShell script (an ordinary API-style write, not synthetic input, the same technique item 6's own verification used) that added then removed a throwaway `VocabularyTerm` entry directly in the running app's real `dictionary.json` — the log showed the exact expected "Dictionary hot-reloaded" sequence plus the honest restart-required warning, both times.
- **Validation-failure-surfacing mechanism:** after writing, waits for the next `DictionaryChanged` event or a 1.5s settle timeout (comfortably longer than the 500ms debounce), then inspects `Current`: entry present with expected shape → success; absent + a matching `RejectedEntries` entry → that rejection's `Reason` shown verbatim (covers per-entry validation failures); absent, no matching rejection, AND the event never fired at all within the timeout → reported as a whole-file duplicate-Id rejection, since a real `DictionaryService` never raises `DictionaryChanged` for that failure mode (per `DictionaryConfig.RejectedEntries`'s own doc comment) — exactly the gap this item's own instructions flagged that `RejectedEntries` alone can't surface.
- **Concurrency, verified by inspection, not just asserted:** the permanent live-refresh subscription (for hand-edits landing while the editor is open) only ever repopulates the entry list from a fresh `Current` read and never touches `StatusMessage`, so it can't clobber a write-then-verify flow's own outcome computation, which always reads its own fresh `Current` snapshot after its own wait settles. A `SemaphoreSlim(1,1)` serializes the whole write-then-verify sequence so two concurrent writes can't misattribute the same next `DictionaryChanged` event to each other's outcome.
- **`DictionaryEditorView.axaml`** replaces the item-4 placeholder: type-filter button row + entry list (edit/toggle/delete per row) on the left, a rejected-entries diagnostics panel in the middle, and a single shared add/edit form panel on the right whose field groups swap visibility by type via computed bool properties (not 5 separate views or a `DataTemplate`-per-type approach — judged simplest for 5 fixed, mostly-2-field shapes). A clear, explicit "changes apply on next restart" notice is shown at the top, matching item 10/Phase 2's already-documented no-live-rebuild limitation. Every color/spacing/dimension value is a `{DynamicResource ...}` token binding; two new component-sizing tokens added to `Elevation.axaml` (`SizeDictionaryListWidth`/`SizeDictionaryFormWidth`).
- **A real XAML build error hit and fixed, not hypothetical:** a `--` inside an actual `<!-- ... -->` XML comment block broke `DictionaryEditorView.axaml`'s parsing with `AVLN1001`/`XmlException` — the same class of bug item 6's own review history already hit once (an XML-comment `--` sequence). Fixed by removing the double-hyphen from the comment text.
- **`FakeDictionaryService`** (new, `tests/Soneto.App.Tests/FakeDictionaryService.cs`, mirroring `FakeHistoryStore`'s pattern) points `DictionaryPath` at a genuine throwaway temp file (the ViewModel's write path is real file I/O, not a service method call) and exposes `SimulateSuccessfulReload`/`SimulateEntryRejectedReload`/`SimulateWholeFileRejected` so a test can drive all three real outcomes deterministically.
- 12 new `DictionaryEditorViewModelTests`: construction reads current entries/rejected entries; adding a valid entry of each of the 5 types produces a correctly-shaped write (verified against the real written+reloaded `Current.Entries`); an invalid add (empty `From`) with a simulated per-entry rejection surfaces the exact reason; a simulated whole-file rejection surfaces the whole-file-rejection message; enable/disable and delete both produce correct writes; an externally-triggered `DictionaryChanged` (a "hand-edit," not the ViewModel's own write) triggers a live refresh marshaled through the injected `uiThreadPost`; the type filter shows only matching rows.
- `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs. `dotnet test soneto.slnx` — 467/467 `Soneto.Core.Tests` (unchanged, zero `Soneto.Core` changes this item), 99 Windows (2 pre-existing skips)/52 Linux unchanged, `Soneto.App.Tests` 25/25 (13 + 12 new).
- **Actually ran `Soneto.App.exe`** and confirmed the full end-to-end hot-reload picture for real (see above). The user's real `dictionary.json` was fully restored to a byte-identical match with the canonical `seed-dictionary.json` afterward (confirmed via `diff`) — no lasting changes to the real environment from this verification.
- **Honest gap, same style as items 3-6's:** no screenshot/visual-inspection tooling available to this implementer session, so the actual rendered appearance of the new Dictionary editor view was NOT visually confirmed by the agent — needs the orchestrating session's/a human's own follow-up pass.
- **Addendum — the orchestrating session's own visual follow-up confirmed the editor renders and functions correctly**, via a real screenshot after a safe, targeted mouse click on the Dictionary nav item (a plain UI-navigation click on the app's own window — not global synthetic input, and distinct from the keyboard-injection attempt this environment's classifier correctly blocked in item 5). Confirmed visible: the page title, the "changes apply on next restart" notice as real UI copy, type-filter tabs (All/Vocabulary/Correction/Regex/Command/Per-app override), add buttons per type, real seed-dictionary spoken-command entries rendered with correct `\n`/`\n\n` display and working Edit/Disable/Delete buttons, and a Rejected-entries panel.
- **One test flake investigated directly and closed**: `AddingEntry_ThatServiceRejects_SurfacesTheRejectionReason` failed once with a raw `IOException` from a bare `File.ReadAllText` call made immediately after `WaitForFileWriteAsync` had already confirmed the file readable — a real TOCTOU gap between the wait loop's own probe-open and the test's separate subsequent read. Reproduced 0/4 additional times across both filtered and unfiltered full-solution re-runs, consistent with a transient Windows AV-scan/file-handle-release timing window rather than a logic bug. Hardened with a new `ReadFileWithRetryAsync` helper mirroring `WaitForFileWriteAsync`'s own retry discipline.
- **Code review then confirmed the real deadlock fix is sound and complete** — the entire awaited call genuinely sits inside the `Task.Run` delegate; `DictionaryService.LoadAsync`/`StartWatching` are confirmed thread-agnostic with zero Avalonia dependency; no other blocking `.GetAwaiter().GetResult()`/`.Wait()` call sites exist anywhere in `Soneto.App`/`Soneto.Composition` — confirmed `Soneto.Daemon` has zero references to `PipelineHost`/`Soneto.App` (grep-verified), and confirmed the write-then-check-for-rejection flow correctly distinguishes per-entry rejection, whole-file rejection, and success with no cross-contamination between concurrent operations (the `_writeGate` semaphore genuinely serializes them). All 5 entry-type forms confirmed to map their fields correctly with no cross-type mixing.
- **No blocking issues; two small should-fixes closed.** (1) `ReadFileWithRetryAsync`/`WaitForFileWriteAsync` only caught `IOException`, not `UnauthorizedAccessException` — widened both, since `DictionaryService.LoadAsync` itself treats the two as the same class of transient failure and the exact flake could otherwise resurface under the other exception type. (2) Save/Edit/Toggle/Delete buttons had no `IsSaving`-driven disabled state during an in-flight write — correctness was never at risk (the write-gate semaphore already serializes concurrent writes correctly) but a rapid double-click gave no visual feedback — added an `IsSaving` property set around each write path, bound via `IsEnabled="{Binding !IsSaving}"` for the Save button (same `DataContext`) and an ancestor-`DataContext` binding (`$parent[UserControl].((vm:DictionaryEditorViewModel)DataContext).IsSaving`) for the per-row Edit/Toggle/Delete buttons inside the list's `DataTemplate`. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — unchanged (467/99[2skip]/52/25). Re-verified with a final live run reaching the same successful pipeline-startup sequence, and a fresh screenshot confirming the new bindings render correctly with no binding errors in the log. **Phase 3 item 7 is done, with a real deadlock bug caught and fixed via genuine live verification, genuine visual confirmation of the finished editor, and two should-fixes closed post-review.**

## 2026-09-02 (Phase 3 item 6)

- **Phase 3 item 6 (History view: list + FTS5 search + per-entry copy + rule-fired diff), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 6 done.**
- **Architecture decision, made explicit up front, same "decided now, not deferred" style as items 0/3/5: history persistence is decoupled from live-session success.** `MainWindow`'s nav pages are built synchronously in `App.axaml.cs`, before `PipelineHost`'s async background pipeline startup has any chance to run or fail — so the real `SqliteHistoryStore` is now constructed EAGERLY there, via a new `HistoryPaths.Resolve()` (`src/Soneto.Core/History/HistoryPaths.cs`, mirroring `ConfigPaths`/`DictionaryPaths` exactly: same override-then-OS-default resolution, `history.db` alongside `config.json`/`dictionary.json`), and passed into `MainWindow`'s (then `HistoryView`'s) constructor — NOT gated behind `PipelineHost.Started`'s success. A user can browse/search past history even in a session where the live pipeline never comes up (e.g. a missing ASR model).
- Separately, `PipelineHost.Started`'s existing handler in `App.axaml.cs` (already there for `RecordingHud.AttachSession`, item 5) now ALSO subscribes `historyStore.AppendAsync` to the now-running `SessionController.DictationCompleted`, fire-and-forget (never awaited inline), per `AppendAsync`'s own "must never throw, safe to call fire-and-forget" contract — no new event/plumbing needed beyond what item 5 already built. `desktop.Exit` disposes the store as best-effort cleanup on a real quit only (never on close-to-tray).
- **`IHistoryStore.Changed` — a small, additive, backward-compatible interface change, the live-refresh mechanism:** a plain `event EventHandler? Changed` added to `IHistoryStore`/`SqliteHistoryStore`, so `HistoryViewModel` never has to talk to `SessionController`/`PipelineHost` directly (only ever `IHistoryStore`). Raised after a SUCCESSFUL `AppendAsync` (not on a failed `-1`), after a `PurgeOlderThanAsync` that actually deleted ≥1 row, and unconditionally after every non-failing `PanicWipeAsync`. Raised AFTER releasing the write `_gate` in every case, and wrapped in its own try/catch that logs and swallows a throwing subscriber — mirroring `SessionController.DictationCompleted`'s own item-2 blocking-bug-fix precedent, applied proactively here.
- **`HistoryViewModel`** (new `src/Soneto.App/ViewModels/HistoryViewModel.cs`, this project's first real ViewModel): a hand-rolled `INotifyPropertyChanged` class (no new MVVM framework dependency), constructor-injected with `IHistoryStore`. Exposes `Entries` (`ObservableCollection<HistoryEntry>`, newest-first), `SearchText`, `SelectedEntry`, and `RawSegments`/`FinalSegments` for the diff. **Both Avalonia-`Dispatcher`-dependent mechanisms (UI-thread marshaling, search debounce) are injectable** via an `internal` test-facing constructor (`uiThreadPost`/`debounceScheduler` delegates), confirmed necessary because constructing a real `DispatcherTimer`/calling `Dispatcher.UIThread` outside a running Avalonia application throws — the public production constructor defaults to a real 400ms restart-on-keystroke `DispatcherTimer` debounce and real `Dispatcher.UIThread.Post`. `RefreshAsync()` is public and directly awaitable by a test, bypassing debounce entirely.
- **Diff rendering uses `AppliedRule`'s structured `From`/`To` spans directly, NOT a text-diff library, per §3.10's own explicit instruction:** `HistoryViewModel.BuildHighlightedSegments(text, spans)` (a pure, `internal static`, independently-unit-tested function) splits `RawText`/`FinalText` into `DiffSegment(Text, IsHighlighted)` records by finding each rule's exact `From`(raw)/`To`(final) substring, leftmost-first, non-overlapping. `HistoryView.axaml` renders each segment list as an `ItemsControl`/`WrapPanel` of `TextBlock`s wrapped in a `Border` that toggles a `Highlighted` style class via Avalonia's `Classes.Highlighted="{Binding IsHighlighted}"` binding shorthand — no converter needed, the highlight color itself stays a plain `{DynamicResource Warning}` style setter.
- **Copy-to-clipboard, Avalonia 12.0.4's real API shape confirmed via reflection against the actually-pinned package (not assumed from an older Avalonia release — this shape genuinely changed):** `IClipboard` no longer has its own `SetTextAsync` (it's now a data-transfer-based API); the string convenience overload is the extension method `Avalonia.Input.Platform.ClipboardExtensions.SetTextAsync(IClipboard, string)`, called via `TopLevel.GetTopLevel(this)?.Clipboard`.
- **A real AVLN3001 build warning hit and fixed, not suppressed:** giving `HistoryView`/`MainWindow` a single required-`IHistoryStore` constructor made Avalonia's XAML compiler warn "no public constructor was found" (needed for avares:// dynamic/design-time XAML resolution) — fixed by adding a second, explicitly-documented-as-never-used-by-production-code public parameterless constructor to each (chained via `: this()`).
- Every color/spacing/corner-radius value in the new view is a `{DynamicResource ...}` binding — zero hardcoded hex/magic-number dimensions; no new sizing tokens were needed (existing `Space*`/`Radius*` tokens covered every new dimension).
- **First real `Soneto.App.Tests` project** (`tests/Soneto.App.Tests/`, plain `net10.0`, xunit/coverlet/Test.Sdk versions pinned to match `Soneto.Core.Tests.csproj` exactly) — the first real UI-adjacent test infrastructure in this project, per §3.15's own "ViewModel logic is unit-tested directly" philosophy. **This explicitly corrects §3.15's own blanket "do not build a `Soneto.App.Tests` project this phase" line** (see that section's own superseded-note in the plan doc): the new project is NOT the UI-automation harness that line was warning against — zero `Avalonia.Headless`/simulated-input/rendering code, only plain xunit tests of `HistoryViewModel` against a new `FakeHistoryStore` in-memory test double. `Soneto.App.csproj` gained an `InternalsVisibleTo` targeting `Soneto.App.Tests`.
- 11 new tests covering: construction loads recent entries and queries with the right blank-query/limit/offset; a `SearchText` change re-queries with the right query after the (synchronously-stubbed) debounce elapses, and does NOT re-query when set to its already-current value; `RefreshAsync()` is directly callable/awaitable bypassing debounce; firing the fake store's `Changed` event triggers a re-query and updates `Entries` (the live-refresh mechanism, proven without any real SQLite/dictation involved); selecting an entry with zero/one/multiple `AppliedRule`s produces correct segment counts/text/highlight-flags on both `RawSegments`/`FinalSegments`; deselecting clears both segment lists; `Dispose()` genuinely unsubscribes from `Changed`. `Soneto.App.Tests` added to `soneto.slnx` under `/tests/`.
- **Verification:** `dotnet build soneto.slnx` — 0 warnings/errors, all TFMs including the new test project (two real build errors hit and fixed along the way: an XML-comment `--` sequence breaking both `HistoryView.axaml` and two `.csproj` files' parsing, and the `IClipboard`/AVLN3001/`Watermark`-obsolete API-shape issues above). `dotnet test soneto.slnx`: `Soneto.Core.Tests` 467/467 (unchanged from item 5's close — this item touched zero `Soneto.Core` behavior beyond the additive `IHistoryStore.Changed` event), `Soneto.Platform.Windows.Tests` 99 (97 + 2 pre-existing hardware-gated skips)/`Soneto.Platform.Linux.Tests` 52/52 unchanged, plus the new `Soneto.App.Tests` 11/11.
- **Actually ran `Soneto.App.exe`** (`dotnet run --project src/Soneto.App -f net10.0-windows`) with the real local model present: the full real pipeline-startup log sequence (config/dictionary load+watch, ASR cold load, VAD load, hotkey source started, `SessionController ready`, `PipelineHost`'s "real pipeline started successfully") completed with zero exceptions, the process stayed alive (~890MB working set) for the full observation window — and, concrete positive confirmation of this item's own architecture decision, `history.db`/`-wal`/`-shm` were created under `%LOCALAPPDATA%\Soneto\` purely as a side effect of `HistoryView`'s construction, proving the real `SqliteHistoryStore`/`HistoryViewModel` path executes correctly, independent of pipeline success. Then terminated cleanly via `taskkill`.
- **Honest gap, same style as items 3/4/5's:** this implementer session has no screenshot/visual-inspection tooling, so the actual rendered appearance of the new History view (list layout, search box, diff-highlight coloring, the copy button) was NOT visually confirmed by the agent — only that the process starts cleanly, the real `IHistoryStore` path executes (proven by the created `history.db`), and the full test suite is green. A real end-to-end hotkey-triggered dictation cycle to visually confirm a live entry appearing in an open History view was deliberately NOT attempted, per this project's standing live-desktop-testing caution — the live-refresh mechanism itself IS verified for real, just via the unit test firing the fake store's `Changed` event directly rather than a real hotkey/dictation cycle. Both gaps need the orchestrating session's/a human's own follow-up pass.
- **Addendum — the orchestrating session's own visual follow-up closed both gaps above, with genuinely strong, concrete confirmation.** Real screenshots confirmed the empty-state History view; a real `SqliteHistoryStore.AppendAsync` call from a throwaway scratch console app (an ordinary, safe API call — NOT synthetic input) against the exact same `history.db` the running app was using succeeded and returned a real row id; and, after restarting the app to force `HistoryViewModel`'s construction-time load to run against the now-populated database, the persisted entry appeared in the list with a working Copy button, and selecting it showed a CORRECT Raw/Final diff with the `AppliedRule`'s `From`/`To` spans genuinely visually highlighted in both columns. One thing that did NOT live-refresh cross-process was diagnosed correct-not-a-bug and confirmed by code review: `IHistoryStore.Changed` is a plain in-memory event on one store instance, not a cross-process signal.
- **Code review then found two real should-fix bugs, both closed same day.** (1) **Debounce/refresh race**: `RefreshAsync` had no ordering guard, so an earlier, SLOWER query's stale results could silently overwrite a later, faster query's correct results (two independent debounce windows, or a live `Changed`-driven refresh racing an in-flight search) — confirmed real and previously untested (the fake store was fully synchronous). Fixed with a monotonically increasing generation counter (`Interlocked.Increment`/`Volatile.Read`) checked after the `await`; a superseded call now discards its own results. New test `StaleSlowerRefresh_DoesNotClobberNewerFasterRefresh` uses a new `FakeHistoryStore.AddGate`/`ReleaseGate` mechanism to construct the exact race. (2) **Diff highlighting over-matched**: `BuildHighlightedSegments` highlighted EVERY occurrence of a rule's `From`/`To` text, even when only one specific occurrence was actually corrected and the same text coincidentally also appeared elsewhere unrelated — directly undercutting the class's own doc-comment claim that structured spans avoid exactly this false-positive risk. Fixed by counting how many times each distinct span appears across `RulesFired` and highlighting only that many leftmost occurrences (a documented, bounded heuristic — not exact position tracking, since `AppliedRule` carries no match offset; extending it was judged disproportionate for this narrow, visual-only edge case, left as an honest residual limitation). New test `SelectingEntry_WithSpanAppearingTwiceButOnlyOneRuleFired_HighlightsOnlyOneOccurrence` proves it. A minor third fix closed an unobserved-exception risk in the fire-and-forget `DictationCompleted → AppendAsync` call (wrapped in a new `AppendHistoryEntrySafelyAsync` helper that catches and logs). `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 467/467 (unaffected), `Soneto.App.Tests` 13/13 (11 + 2 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52). Re-verified with a final live pipeline run. **Phase 3 item 6 is done, with genuine end-to-end visual confirmation of the full persistence/search/diff pipeline and two real bugs caught and fixed before shipping.** Next: item 7 (Dictionary editor).

## 2026-09-02 (Phase 3 item 5)

- **Phase 3 item 5 (Recording HUD + live level meter), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 5 done.**
- **Ordering decision, made explicit now (mirroring items 0/3's own "decided now, not deferred" style):** no prior item wired the real dictation pipeline (`SessionController`, real audio capture, the real global hotkey) into `Soneto.App` — items 5/6/8/9 all implicitly assumed one existed, but item 5 is the first that actually needs a running session, so it does that wiring once, as its own explicit sub-task. Item 6 (History) will reuse the SAME already-running `SessionController` instance to subscribe to `DictationCompleted`, not re-wire anything.
- **Sub-task A — real pipeline wiring:** new `Soneto.App/PipelineHost.cs` mirrors `Soneto.Daemon/Program.cs`'s own real daemon-startup call sequence exactly (`DaemonComposition.CreateConfigService`/`CreateDictionaryService`/`LoadAndStartWatchingConfigAsync`/`LoadAndStartWatchingDictionaryAsync`/`CreatePlatformHotkeySourceAndTextInjector`/`BuildAndStartSessionControllerAsync`) rather than re-deriving it — zero duplicated composition logic. Called as a genuine fire-and-forget background `Task` from `App.axaml.cs` AFTER the main window/tray are already constructed and shown, with an outermost `try/catch` layered on top of `BuildAndStartSessionControllerAsync`'s own already-established never-throws contract — on any failure it logs critical and simply never raises `Started`, leaving the HUD inactive rather than crashing the app. `ConfigPaths.Resolve()`/`DictionaryPaths.Resolve()` used exactly as `Soneto.Daemon` uses them.
- **`DaemonComposition.BuildAndStartSessionControllerAsync` widened from a 2-tuple to a 3-tuple** — `(Controller, CuePlayer)` → `(Controller, CuePlayer, AudioCapture)` (a plain named tuple, not a new record, since a tuple already self-documents each field) — so `Soneto.App` can subscribe to the `PortAudioCapture` instance's `LevelChanged` event that method already builds internally but never exposed. Every failure branch's third slot is `null`; the success branch's third slot is the real constructed capture instance; no other line of the method's body touched. `Soneto.Daemon/Program.cs`'s call site updated to discard the new third value (`(sessionController, sessionCuePlayer, _) = await ...`). **Confirmed zero functional diff to `Soneto.Daemon`:** it still builds with 0 warnings/errors; the full pre-existing test suite (466 `Soneto.Core.Tests` + 99 Windows [2 pre-existing skips] + 52 Linux) is completely unchanged from item 4's close; and a real `Soneto.App` run (which now calls the identical `DaemonComposition` entry points) shows the same log-line sequence item 0's own daemon smoke test already established.
- `Soneto.App` gained a bare `Microsoft.Extensions.Logging` console logger (it had none before — `LoggerFactory.Create(b => b.AddSimpleConsole(...))`, new `Microsoft.Extensions.Logging.Console` package reference) — deliberately NOT a full Serilog setup like `Soneto.Daemon`'s, per this item's own "disproportionate for this item" instruction. `Soneto.App.csproj` gained `ProjectReference`s to `Soneto.Composition`/`Soneto.Platform.Linux`/`Soneto.Platform.Windows` (the last TFM-conditioned to `net10.0-windows`), mirroring `Soneto.Daemon.csproj`'s reference shape exactly.
- **Pipeline-startup verification, done via real log inspection, never via a triggered real dictation — per this item's own explicit safety instruction:** ran `Soneto.App.exe` twice from a real terminal with the repo's real local model present (`models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v3-int8/`). Both runs show the full real success sequence in order — config/dictionary load+watch, repo-local dev model dir found, ASR cold model load in ~2.3-2.6s, warm-up decode, Silero VAD load, `WindowsHotkeySource` started with the real configured trigger, `SessionController ready`, `PipelineHost`'s own "real pipeline started successfully" line — with zero exceptions, and the process stayed alive (~886-907MB working set, consistent with a loaded ASR model) for the full ~18-20s observation window each time before a clean `taskkill`. This confirms the "succeeds cleanly" outcome (the model IS present in this environment, so the graceful-failure branch wasn't separately exercised live here, but that code path is unchanged from item 0's own already-verified never-throws contract).
- **Sub-task B — the HUD itself:** new `Views/RecordingHud.axaml`(`.cs`) — a borderless (`WindowDecorations="None"`; Avalonia 12.0.4 renamed the now-obsolete `SystemDecorations` to this, caught as a real build warning and fixed, not assumed), `ShowInTaskbar="False"`, `Topmost="True"`, non-resizable window.
- **No focus stealing, verified against the actually-pinned Avalonia version via real reflection, not memory:** `Window.ShowActivated` (confirmed via reflection against the pinned 12.0.4 package to be a genuine `bool` property on `Avalonia.Controls.Window`) is set `False` in XAML, so `Show()` never activates/focuses the HUD.
- **Positioning — fixed bottom-right corner of the primary screen, the implementer's documented choice per §3.9's own "implementer's call, document the choice" allowance:** simpler and fully deterministic than cursor-tracking, with no dependency on a cursor-position API; computed via `Screens.Primary`/`WorkingArea` each time the HUD is shown (screen geometry can only be read once the window exists).
- Live level meter: a plain two-`Border` fill bar (a fixed-width "track" Border plus a `Success`-colored inner Border whose `Width` is set directly from the fill ratio in code) rather than a `ProgressBar`, for direct token-driven `CornerRadius`/color control. `DbfsToFillRatio(double dbfs)` — a small `internal static` pure function (-60dBFS floor, 0dBFS ceiling, both clamped, `NaN`/`-Infinity` → 0) with its own short XML doc comment — is this item's one piece of testable logic. **Kept correct-by-inspection rather than covered by a new `Soneto.App.Tests` project — a deliberate, documented choice, not an oversight:** standing up a whole new UI test project for one small conversion function is disproportionate for this item alone; item 6/History's real, test-worthy ViewModel is where real `Soneto.App` unit test infrastructure should actually get built, per §3.15.
- Elapsed-time counter: a plain once-per-second `DispatcherTimer`, deliberately NOT tied to the ~20Hz level updates. No language chip — §3.1's own explicit scope decision (no classifier exists). Every color/spacing/corner-radius/shadow value is a `{DynamicResource ...}` token binding; four new component-sizing tokens added to `Elevation.axaml` (`SizeHudWidth`/`SizeHudHeight`/`SizeHudMeterWidth`/`SizeHudMeterHeight`), matching every prior view's "named by role" sizing-token precedent.
- **Threading — the exact requirement this item's own instructions flagged as historically easy to miss, done right and demonstrated, not just asserted:** `SessionController.StateChanged` (session worker thread) and `IAudioCapture.LevelChanged` (audio capture callback thread) both fire on non-UI threads; every `RecordingHud` event handler does only pure off-thread data extraction and marshals every actual control mutation through `Dispatcher.UIThread.Post`.
- **HUD logic verified via a temporary, since-deleted debug harness — per this item's explicit safety constraint against a real end-to-end hotkey-triggered dictation cycle** (which could paste into whatever has real OS focus on this shared machine): a `--debug-hud-harness` CLI-gated code path in `App.axaml.cs` plus three `internal DebugSimulateShow`/`DebugSimulateLevel`/`DebugSimulateHide` passthrough methods on `RecordingHud` calling its own private `ShowAtBottomRightOfPrimaryScreen`/`UpdateLevelFill`/`HideHud` directly — no real `SessionController`, hotkey, or microphone involved at any point. A real run of `Soneto.App.exe -- --debug-hud-harness` showed the HUD appear ~1.5s after startup, drove 20 simulated level updates ramping from -60dBFS to -3dBFS over ~3s (each crossing to the UI thread via `Dispatcher.UIThread.InvokeAsync`, exactly like the real `LevelChanged` handler would), waited 2s (long enough for the once-per-second elapsed timer to tick at least once), then hid the HUD — all while the real pipeline startup sequence above was ALSO running concurrently in the background, proving both code paths coexist safely. The process stayed alive with zero exceptions for the full observation window; since Avalonia throws loudly and immediately on a genuine cross-thread control-access violation, this clean run is real positive evidence the `Dispatcher.UIThread` marshaling is correct, not just an absence-of-crash inference. **The entire debug harness was deleted immediately after this one verification run**, per this item's own explicit instruction — `Soneto.App` ships with none of it.
- `dotnet build soneto.slnx` — 0 warnings, 0 errors (both TFMs) — the one Avalonia-obsolete-API warning hit along the way (`SystemDecorations`) was fixed, not suppressed. `dotnet test` — `Soneto.Core.Tests` 466/466, `Soneto.Platform.Windows.Tests` 99 (97 + 2 pre-existing hardware-gated skips), `Soneto.Platform.Linux.Tests` 52/52 — all unchanged from item 4's close, exactly as expected per this item's own documented "correct-by-inspection for `DbfsToFillRatio` this item" choice.
- **Honest gap, not silently skipped, exactly as this item's own instructions required:** a real, live, hotkey-triggered visual confirmation (holding the actual configured trigger key and watching the HUD genuinely appear bottom-right with a real live-updating meter and elapsed counter, then disappear on release) was deliberately NOT attempted by this implementer session — per this item's own explicit safety instruction, that one live check is left for the orchestrating session or a human to do carefully, e.g. with a deliberately very brief press well under `MinDurationMs` so no real transcription/injection can ever occur even if the mechanism does trigger. - **Addendum — the orchestrating session's own attempt at the live hotkey check was correctly BLOCKED by this environment's permission-classifier.** A genuine `SendInput`-based synthetic RightControl press (a deliberately silent ~600ms hold, chosen specifically so VAD's already-well-tested silence-discard path would prevent any transcription/injection even if the recording completed) was flagged and denied before it could execute — the environment's own safety gate treated synthetic keyboard injection as needing human judgment, which is exactly the right call given this project's own documented near-misses with synthetic input. The gap remains real and open: nobody has yet visually confirmed the HUD appears/disappears from a genuine hotkey press. **Given that, code review specifically scrutinized every code path only a real event (not the implementer's own direct-method-call debug harness) would exercise, and confirmed by careful inspection**: `AttachSession` genuinely subscribes to the real `SessionController.StateChanged`/`IAudioCapture.LevelChanged` events, correctly shows on `Recording` and hides on `Idle`/`Faulted`; every ONGOING event handler (not just the one-time `PipelineHost.Started` attach) correctly marshals through `Dispatcher.UIThread.Post` before touching any control; and all four of `BuildAndStartSessionControllerAsync`'s widened return statements were traced individually and confirmed consistent — no failure branch can return a non-null `AudioCapture` alongside a null `Controller`/`CuePlayer`. No blocking issues found. **Two should-fix items closed**: (1) the widened 3-tuple had zero direct test coverage (the only existing test re-implemented the catch shape locally, with a comment that had gone stale referencing the old 2-tuple) — added `RealBuildAndStartSessionControllerAsync_ModelMissing_ReturnsAllNullTuple_NeverThrows`, which calls the ACTUAL `DaemonComposition.BuildAndStartSessionControllerAsync` with a genuinely missing model directory and asserts the real 3-tuple comes back all-null without throwing (required a new `Soneto.Composition` project reference on `Soneto.Core.Tests`, safe since `Soneto.Composition`'s plain `net10.0` build has no Windows-platform dependency); fixed the stale comment too. (2) `AttachSession` had no re-entrancy guard — harmless today since its one call site is a one-shot event, but a cheap `_attached` flag was added against a future change that might fire it more than once. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 467/467 (466 + 1 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52). Re-verified with a final live run showing the identical successful pipeline-startup sequence. **Phase 3 item 5 is done, with pipeline-startup and HUD-logic both genuinely verified within this session's real safety constraints, and the one remaining real-hotkey visual check explicitly and honestly deferred to a human.** Next: item 6 (History view: list + search (FTS5) + diff).

## 2026-09-02 (Phase 3 item 4)

- **Phase 3 item 4 (tray icon + main-window nav-rail shell), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 4 done.** Extends the already-existing `Soneto.App` project (item 3's decision) — does not create a second project.
- **Tray icon — Avalonia 12.0.4's real API shape, confirmed via reflection against the actually-pinned package version, not assumed from a different Avalonia release:** `Avalonia.Controls.TrayIcon` is a standalone `AvaloniaObject` attached to the running `Application` via the static `TrayIcon.SetIcons(Application, TrayIcons)`; icon is a `WindowIcon`; left-click is `TrayIcon.Clicked`; the right-click menu is a `NativeMenu` (supports collection-initializer syntax via its own `Add(NativeMenuItemBase)`) of `NativeMenuItem`s (`Click` event, `IsChecked`/`ToggleType=MenuItemToggleType.CheckBox` for the checkable "Pause dictation" item) + `NativeMenuItemSeparator`s.
- `App.axaml.cs`'s new `SetupTrayIcon`: "Open Soneto" calls a new `MainWindow.ShowAndActivate()` (`Show()`+`WindowState=Normal`+`Activate()`); "Pause dictation" is a checkable toggle flipping a private `_isPaused` bool and its own `IsChecked` — explicitly commented as having NO real effect yet since `SessionController`/the real daemon composition isn't wired into `Soneto.App` (items 10/11's job), deliberately not stubbed with a fake session; "Quit" sets a private `_isQuitting` flag then calls `desktop.Shutdown()`. Left-click also calls `ShowAndActivate()`.
- **Placeholder tray icon, clearly documented as such:** `Assets/tray-icon.ico` — a generated 32x32 solid-color circle (the `Accent` light-theme token color, `#FF3D6BFF`) with a white outline, PNG-in-ICO format, referenced via `avares://Soneto.App/Assets/tray-icon.ico`; `Assets/README.md` states explicitly this is a placeholder, not real branding (none exists/is called for anywhere in Phase 3). `Soneto.App.csproj` gained `<AvaloniaResource Include="Assets\*.ico" />` (the hand-adapted `.csproj` from item 3 doesn't auto-include an `Assets` folder the way the stock template does).
- **Main-window nav-rail shell:** `MainWindow.axaml` is now a `Grid` with a fixed-width nav rail (`ListBox`, 4 items — History/Dictionary/Settings/Permissions, in that exact order per §3.8's own "History first, most-used surface day to day" rationale) and a `ContentControl` swapping `Content` on `SelectionChanged` by indexing into a small cached `Control[]` built once in the constructor — no navigation-service abstraction, per §3.8's own explicit instruction that 4 static destinations don't need one. Four new genuinely empty placeholder `UserControl`s (`Views/HistoryView`, `DictionaryEditorView`, `SettingsView`, `PermissionsDoctorView`) — a title + "coming in item N" line, zero real functionality, per this item's scope boundary against items 6-9's own later, larger specs. `KitchenSinkView.axaml` (item 3) stays in the repo untouched, just no longer `MainWindow`'s content. New `SizeNavRailWidth` component-sizing token added to `Elevation.axaml` (same "named by role" precedent as item 3's `SizeSwatchWidth` etc.) — every new dimension/color value is a `{DynamicResource ...}` binding, zero hardcoded hex/magic numbers.
- **A real bug hit and fixed during this item's own verification, not left for review:** binding `SelectedIndex="0"` directly on the nav `ListBox` in XAML fired `SelectionChanged` mid-`InitializeComponent` (`EndInit`), before the `NavContent` named field was assigned by the XAML parser, causing an unguarded `NullReferenceException` crash on every launch. Fixed by removing the XAML-declared `SelectedIndex` and instead setting `NavList.SelectedIndex = 0` in the constructor AFTER `InitializeComponent()` returns.
- **Close-to-tray, with the honest configurability gap named explicitly rather than ignored or over-built:** `MainWindow.Closing` now cancels + `Hide()`s unless `_isQuitting` is set (only by the tray's "Quit" item), so the window's X button minimizes to tray while "Quit" genuinely exits the process. §3.8 also asks for this to be "configurable in Settings... don't hardcode it as the only behavior" — correctly NOT built here: `Soneto.App` doesn't read `config.json`/wire up `SonetoConfig`/`IConfigService` at all yet (item 8's job), so a config-backed toggle would have nothing real to bind to; a fake/unwired settings toggle just to satisfy the letter of "configurable" would have been scope creep. Documented as a real, named gap (mirroring Phase 1's items 11/12 and Phase 2's "no live rebuild" gap style) directly in `App.axaml.cs`'s own comment: minimize-to-tray is the correct, current, FIXED default for now; item 8 must make it a genuine config-backed setting once `SonetoConfig`/`IConfigService` are actually wired into `Soneto.App`.
- **Verification:** `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs. Actually ran the app (`dotnet run --project src/Soneto.App -f net10.0-windows`) and confirmed via `tasklist` that `Soneto.App.exe` started, stayed alive (~146-154MB working set) for an ~18-second observation window with zero crash/error output (post-fix — the pre-fix run crashed immediately on the bug above, log captured and used to diagnose it), then terminated cleanly via `taskkill`.
- **Honest gap, same shape as item 3's:** this environment has no screenshot/visual-inspection tooling available to the implementer agent, so the tray icon's actual visibility/clickability, the nav rail's actual visual swap-on-click behavior, and close-to-tray's actual visual effect were NOT confirmed by the agent — only that the process builds the tray icon/menu/nav shell without a startup crash and stays alive without a runtime exception. This needs the orchestrating session's own visual follow-up pass, per §3.15's "visual stuff verified by a human" precedent item 3 already established.
- Full pre-existing test suite confirmed completely unaffected, exactly per §3.15's own "no meaningful automated UI test to add for tray/window-chrome behavior" expectation: `dotnet test soneto.slnx` — `Soneto.Core.Tests` 466/466, `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 pre-existing hardware-gated skips), `Soneto.Platform.Linux.Tests` 52/52 — all unchanged from item 3's close. - **Addendum — the orchestrating session's visual follow-up confirmed the nav shell renders correctly**: a real screenshot showed the "Soneto" title bar, all four nav destinations, "History" selected by default with the correct placeholder text. Tray icon on-screen visibility was inconclusive from a screenshot alone (likely tucked behind Windows' default overflow chevron for a new app's first tray icon, normal behavior); treated as reasonably confirmed via the implementer's reflection-verified correct Avalonia 12.0.4 API usage plus a clean, error-free startup. **Code review then traced the single most important interaction in this item — does Quit actually quit, given close-to-tray intercepts the window's `Closing` event — and confirmed it does**: `Quit` sets an `_isQuitting` flag and calls `desktop.Shutdown()` directly, never `window.Close()`; the `Closing` handler checks that flag first and only intercepts (cancel + `Hide()`, not minimize) when it's false — no race, since both run synchronously on the UI thread. Review found no other blocking issues (the cached-pages array is built once and correctly ordered; "Pause dictation" is genuinely inert with no scope creep; all four placeholder views hold to the token-only discipline; the placeholder tray icon is a reasonable 860 bytes). **One should-fix applied**: `OnNavSelectionChanged` relied entirely on the constructor's ordering discipline to avoid the exact `NullReferenceException` already caught and fixed once during the implementer's own verification — added a defensive `NavContent is null` guard so a future accidental XAML regression (e.g. reintroducing `SelectedIndex="0"` in XAML) fails safely instead of crashing on every launch again. Re-verified: `dotnet build soneto.slnx` 0 warnings/errors, full test suite unchanged (466/99[2skip]/52). **Phase 3 item 4 is done, with genuine visual confirmation and the Quit-vs-close-to-tray interaction specifically traced and confirmed correct.** Next: item 5 (Recording HUD + live level meter).

## 2026-09-02 (Phase 3 item 3)

- **Phase 3 item 3 (design tokens), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 3 done.** **Ordering decision, made explicit now (mirroring item 0's own §3.3/§3.4 "decided now, not deferred" style):** item 3's "done when" bar needs a rendering kitchen-sink view, which needs an Avalonia project — the plan's original sketch didn't create `Soneto.App` until item 4. **Decision: item 3 creates the real `Soneto.App` project now**, and item 4 EXTENDS it (tray icon, nav-rail shell) rather than creating a second project.
- New `src/Soneto.App/Soneto.App.csproj`: `WinExe`, `net10.0;net10.0-windows`, the `WINDOWS`-symbol-fix `PropertyGroup` copied verbatim from `Soneto.Daemon.csproj` (same documented SDK gap for that TFM). `ProjectReference` to `Soneto.Core` only, added now because every later item in this phase needs it (deliberately not `Soneto.Composition`/platform projects yet — item 4's job). Avalonia packages `Avalonia`/`Avalonia.Desktop`/`Avalonia.Themes.Fluent`/`Avalonia.Fonts.Inter`, all pinned to `12.0.4` (current latest stable, confirmed via `dotnet new avalonia.app`'s generated project). Added to `soneto.slnx` under `/src/`.
- **Templates WERE available** in this environment (`avalonia.app`/`avalonia.mvvm`/`avalonia.xplat` all present in `dotnet new list`) — probed with a scratch `dotnet new avalonia.app`, then hand-adapted its generated `App.axaml`/`App.axaml.cs`/`Program.cs`/`MainWindow` shape to this project's own conventions (`Views/` subfolder, no `AvaloniaUI.DiagnosticsSupport` package) rather than writing entirely from scratch.
- `src/Soneto.App/Styles/`: `Colors.axaml` (`Background`/`Surface`/`TextPrimary`/`TextSecondary`/`Accent`/`Success`/`Warning`/`Danger`/`Border`, `SolidColorBrush` resources inside `ResourceDictionary.ThemeDictionaries` keyed `Light`/`Dark`); `Typography.axaml` (a `Styles` file, not a plain `ResourceDictionary` — type-scale values exposed both as raw resources and as `TextBlock.Display/.Title/.Body/.Caption` style classes built from them, plus one `Inter` font-family resource); `Spacing.axaml` (4/8/12/16/24/32px as `SpaceXs`..`SpaceXxl` doubles plus matching `Thickness` resources); `Elevation.axaml` (`RadiusSm`/`RadiusMd`/`RadiusLg`, `ShadowCard`/`ShadowRaised`); `Motion.axaml` (`MotionFast`/`Normal`/`Slow` `TimeSpan`s at 100/200/350ms plus `CubicEaseInOut`/`CubicEaseOut` easing instances). All five wired into `App.axaml`'s `Application.Resources`/`Application.Styles`.
- **A real build error hit and fixed along the way:** `Motion.axaml`'s easing resources initially declared the `Avalonia.Animation.Easings` XML namespace inline on each `<CubicEaseInOut>`/`<CubicEaseOut>` element; Avalonia's XAML parser rejects any non-root-element `xmlns` declaration with `AVLN2000` ("xmlns declarations are only allowed on the root element to preserve memory"). Fixed by declaring the namespace once at the `ResourceDictionary` root instead.
- `Views/KitchenSinkView.axaml(.cs)` — the scratch kitchen-sink view, shown as `MainWindow`'s `Content` for now — renders every token: one swatch+label per color (in a `WrapPanel`), one sample `TextBlock` per typography step, a row of `Border`s sized by each spacing token, two elevation cards (`RadiusSm`+`ShadowCard`, `RadiusLg`+`ShadowRaised`), and two motion demos (a `MotionFast`-duration hover-fade button local style, plus a `MotionNormal`-duration whole-page fade-in on `Loaded`), with a runtime "toggle light / dark theme" button that flips `Application.Current.RequestedThemeVariant`. Every single property value in this file — colors, margins, paddings, corner radii, shadows, durations — is a `{DynamicResource ...}` binding; zero hardcoded hex colors or magic-number margins, establishing the "views pull from tokens" discipline as an actual precedent from the first view rather than an afterthought.
- `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs, `Soneto.App` included. **Actually ran the app** (`dotnet run --project src/Soneto.App -f net10.0-windows`); confirmed via `tasklist` that `Soneto.App.exe` started and stayed alive (~185-192MB working set) for a ~10-second observation window with zero crash/error output in the redirected log, then terminated cleanly on `taskkill`. **Honest gap:** this environment has no screenshot/visual-inspection tooling available to the agent, so the actual RENDERED appearance (colors, layout, the theme toggle's visual effect, the fade-in animation) was NOT visually confirmed by the agent — only that the process starts, stays alive without an `XamlParseException`/missing-font crash, and exits cleanly. A human should do a quick visual pass before item 4 builds on top of this, per §3.15's "visual stuff verified by a human" philosophy.
- Full pre-existing test suite unaffected, exactly as expected for a new UI-only project with no automated UI test suite added per §3.15's own testing philosophy: `dotnet test` — `Soneto.Core.Tests` 466/466 (unchanged from item 2's close), `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 pre-existing hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 52/52 (unchanged).
- **Addendum — the visual-confirmation gap was closed same day.** The orchestrating session (which had screenshot capability the sandboxed implementer agent lacked) launched the app itself and captured a real screenshot via `System.Drawing`/`System.Windows.Forms.Screen`. The "Soneto design tokens" kitchen-sink window rendered correctly: all nine color swatches distinctly colored and legibly labeled, all four typography steps visually distinct in size/weight, the spacing scale showing visibly-increasing blocks, and the "toggle light / dark theme" button present and legible — genuine confirmation the design-token discipline works end-to-end, not just that the process didn't crash. The screenshot (which necessarily captured the full desktop) was deleted immediately after review and the test process cleanly terminated. - **Addendum — code review found one real should-fix.** `KitchenSinkView.axaml`'s own header comment claimed "zero hardcoded hex colors or magic-number margins," but `Width`/`Height`/`BorderThickness` on the color swatches (64/48/1), spacing demo bars (32), and elevation cards (200) were hardcoded literals with no matching token — the letter of the "margin" claim held (margins/padding genuinely were all `DynamicResource`), but this is exactly the magic-number pattern §3.7's discipline exists to prevent, left in the very view meant to set the precedent for every future view in this phase. Fixed by adding four component-sizing tokens (`SizeSwatchWidth`/`SizeSwatchHeight`/`SizeDemoBarHeight`/`SizeDemoCardWidth`, named by role, not raw pixel value) plus a `BorderThicknessHairline` token to `Elevation.axaml`, then rebinding every affected value in `KitchenSinkView.axaml` — confirmed zero remaining hardcoded numeric `Width`/`Height`/`BorderThickness` in the file (only the design-time-only `d:DesignWidth`/`d:DesignHeight` IDE-preview hints remain, never rendered at runtime). Re-verified with a second real screenshot after the fix: identical, correct rendering — swatches/typography/spacing bars all unchanged visually, now fully token-driven. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — unchanged (466/466 + 99[2skip]/52). **Phase 3 item 3 is done, with genuine visual confirmation both before and after the fix.** Next: item 4 (`Soneto.App` tray icon + main-window nav-rail shell — extending this project, not creating a new one).

## 2026-09-02 (Phase 3 item 2)

- **Phase 3 item 2 (`SessionController.DictationCompleted`), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 2 done.** The ONE deliberate touch to `src/Soneto.Core/SessionController.cs` this phase makes, per §3.2/§3.17's own explicit "highest-risk item in the phase" framing. Added `DictationCompletedEventArgs` (`RawText`/`FinalText`/`RulesFired`/`RecordingDuration`/`ProcessingLatency`/`WasInjected`) and `public event EventHandler<DictationCompletedEventArgs>? DictationCompleted`, doc-commented to mirror `StateChanged`'s own "raised on the session worker thread, handlers should not block" convention.
- **Exactly one raise-site.** A new private `RaiseDictationCompleted` helper is called from all three of `RunInjectingAsync`'s terminal branches — the `InjectionOutcome.Injected` branch, the "any other outcome" branch, and the `catch (Exception ex)` branch (injection threw) — always BEFORE `EnterCooldownAsync()` so subscribers observe the event promptly rather than after the 150ms cooldown delay. `WasInjected` is `true` only for the `Injected` outcome, `false` for every other outcome and for the thrown-exception case, since in all three cases post-processed final text genuinely existed and injection was genuinely attempted.
- **Confirmed by tracing the real call chain that the event deliberately does NOT fire from any of the six "nothing to inject" exit paths**, since none of them ever reaches `RunInjectingAsync`: `HandleKeyUpAsync`'s below-`MinDurationMs` discard and `FinalizeRecordingAsync`'s "audio device lost" catch both call `EnterCooldownAsync()` directly; `RunFinalizingAsync`'s VAD-discard branch calls `EnterCooldownAsync()` before ever calling `RunTranscribingAsync`; `RunTranscribingAsync`'s transcription-exception/timeout catch and its empty-result branch both call `EnterCooldownAsync()` before ever calling `RunInjectingAsync`.
- **Data threading kept deliberately mechanical, per the item's own instruction not to restructure the call chain's shape.** `FinalText`/`RawText` needed no new plumbing at all: `FinalText` is `RunInjectingAsync`'s pre-existing `toInject` local, and `RawText` is `transcriptionResult.Text`, already a parameter there — confirmed correct by reading `TranscriptionResult`'s definition (`Soneto.Core.Asr`) and verifying it's an immutable `sealed record`, so `transcriptionResult.Text` in `RunInjectingAsync` is provably the same value `result.Text` was in `RunTranscribingAsync` before post-processing ran, not something that could have been mutated/reused in between. `RulesFired` (`postProcessed.Applied`) is a genuinely new parameter threaded from `RunTranscribingAsync` into `RunInjectingAsync`. `RecordingDuration` reuses the EXISTING `recordingDuration` local already computed once in `FinalizeRecordingAsync` for the long-utterance-cue log check (correct uniformly for both the key-up and max-duration-elapsed callers) — threaded down through `RunFinalizingAsync` → `RunTranscribingAsync` → `RunInjectingAsync` as a new parameter, not recomputed via a second timestamp scheme. `ProcessingLatency` ("key-up/forced-finalization → injected", per §3.5's framing) is measured with a new `Stopwatch` started right where `recordingDuration` is computed in `FinalizeRecordingAsync` (as processing genuinely begins) and read via `.Elapsed` at the actual raise-site in `RunInjectingAsync` — mirroring the `sw`/`decodeElapsed` local-`Stopwatch`-threading idiom `RunTranscribingAsync`/`RunInjectingAsync` already used for their own internal timing, just extended one level further down. `RunFinalizingAsync`/`RunTranscribingAsync`/`RunInjectingAsync`'s private signatures each grew two-to-three additive parameters; no other line in the file was touched.
- **9 new tests added to `tests/Soneto.Core.Tests/SessionControllerTests.cs`**, using the file's existing fakes/harness with no parallel test infrastructure: the event fires exactly once with correct data (`RawText != FinalText` and a non-empty `RulesFired`, using a `FillerWordStripper`-backed `PostProcessorChain` for a genuinely non-trivial diff rather than a no-op pass-through chain) on a real key-down→speak→key-up→transcribe→post-process→inject flow, with `RecordingDuration` checked against a real ~200ms wall-clock delay between press/release and `ProcessingLatency` checked non-negative; fires with `WasInjected=false` for a non-`Injected` outcome, and separately for injection throwing, both still carrying correct raw/final/rules data; and does NOT fire for all six traced "nothing to inject" exit paths (below-`MinDurationMs` discard, VAD-discarded silence, empty transcription result, transcription exception, transcription timeout, audio-device-lost). The full pre-existing `SessionControllerTests.cs` suite (every prior test, left completely unmodified) still passes — zero regressions to the 850-line state machine.
- `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 464/464 (455 + 9 new), `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 pre-existing hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 52/52 (unchanged) — matches the pre-item-2 baseline exactly aside from the 9 new tests.
- **Addendum — given this item's own stated highest-risk status in the whole phase, both verification and code review scrutinized it hard, and code review caught a real BLOCKING bug before it shipped.** Verification's fresh call-chain re-trace found a 7th non-firing exit path the doc comment hadn't listed (`HandleHookFaultedAsync` aborting mid-recording — the behavior was already correct, only the doc comment was incomplete; fixed). **Code review then found the real bug:** `RaiseDictationCompleted` was called unguarded, immediately BEFORE the mandatory `EnterCooldownAsync()` call — `SessionController`'s only path back to `Idle` — on both its call sites in `RunInjectingAsync`. A throwing `DictationCompleted` subscriber would unwind straight past `EnterCooldownAsync()` (no other catch exists between there and `RunWorkerLoopAsync`'s outer per-command catch, which logs and moves on WITHOUT ever restoring `State`), permanently stranding the session in `Injecting` — every subsequent key-down silently ignored by `HandleKeyDownAsync`'s `State != Idle` guard until the whole daemon process is restarted. This is a materially worse risk than the already-accepted `StateChanged` "handlers should not block" precedent: `StateChanged` fires AFTER its own state write already lands, while `DictationCompleted` fires BEFORE a still-pending mandatory state-recovery step. **Fixed** by wrapping the `Invoke` call in a try/catch inside `RaiseDictationCompleted` (log and continue to cooldown regardless), matching the same defensive-backstop discipline this class already uses elsewhere (`SafeAbortCaptureAsync`, `HandleHookFaultedAsync`'s per-attempt catch). New regression test `DictationCompleted_ThrowingSubscriber_StillReachesIdle_NotStrandedInInjecting` proves a throwing handler can no longer strand the session, AND that a subsequent key-down still starts a genuinely new recording afterward (not just a transient `Idle` visit). Also closed a should-fix on the test side: the original `ProcessingLatency` ordering test used a small, near-symmetric recording/delay pair (~50ms/~300ms) that couldn't actually distinguish a correct independent-measurement implementation from a hypothetically-buggy additive one (`RecordingDuration + postRecordingElapsed`) — both would've landed in the same range with those numbers. Rewritten with a large, deliberately asymmetric pair (~1000ms recording/~150ms delay) plus BOTH a lower and upper bound on `ProcessingLatency`, so the additive-bug hypothesis (~1150ms) clearly violates the upper bound (<600ms) while the correct implementation (~150-300ms) satisfies it. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 466/466 (464 + 2 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52). **Phase 3 item 2 (`SessionController.DictationCompleted`) is done — the phase's highest-risk item, and the extra scrutiny it got paid off with a real bug caught before shipping.**

## 2026-09-02 (Phase 3 item 1)

- **Phase 3 item 1 (`IHistoryStore` + SQLite/FTS5 schema), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 1 done.** New `src/Soneto.Core/History/` folder: `HistoryEntry.cs` (the record, matching §3.5 exactly), `IHistoryStore.cs` (`AppendAsync`/`SearchAsync`/`PurgeOlderThanAsync`/`PanicWipeAsync`), `SqliteHistoryStore.cs` (the implementation). Added `Microsoft.Data.Sqlite` 10.0.0 to `src/Soneto.Core/Soneto.Core.csproj`, plus a direct `SQLitePCLRaw.bundle_e_sqlite3` 3.0.5 override of the version Microsoft.Data.Sqlite otherwise transitively pulls (2.1.11, which carries a known high-severity NU1903 advisory, GHSA-2m69-gcr7-jv3q) — both pure managed/P-Invoke with no platform-specific surface, confirmed legitimate for `Soneto.Core` per this project's "no platform assembly referenced" rule.
- **FTS5 availability verified for real, per §3.5's own explicit instruction — the finding is that it works.** `CREATE VIRTUAL TABLE ... USING fts5(...)` succeeds on this machine's package set, and a real `MATCH` query against real persisted content in `SqliteHistoryStoreTests` returns exactly the matching row and excludes the non-matching one — not just "schema creation didn't throw." The `LIKE '%term%'` fallback the plan asks for as a contingency exists in code (a caught `SqliteException` around the FTS5 DDL flips an internal `_ftsAvailable` flag, used by `SearchAsync`), but is NOT the path this build actually ships on — documented honestly in `SqliteHistoryStore`'s own class doc comment rather than silently assumed.
- **Schema matches §3.5's sketch exactly**: `history` table (7 columns + autoincrement `id`), `history_fts` as an FTS5 external-content table over `final_text` keyed to `history.id`, and three hand-written insert/update/delete triggers keeping them in sync — all `IF NOT EXISTS`, safe to construct repeatedly against an existing populated database. `PanicWipeAsync`'s bulk-`DELETE FROM history`-vs-trigger question was verified directly (not assumed): a real search for content that existed before a panic wipe returns nothing afterward, confirming the `AFTER DELETE` trigger fires per-row even for a WHERE-less bulk delete and leaves no stale `history_fts`-only phantom hits.
- **`HistoryEntry.Id` sentinel decision, made deliberately and documented in the record's own doc comment**: one type reused for both a new (not-yet-persisted) entry and a persisted one, with `Id = 0` as the "not yet persisted" sentinel, rather than a second no-Id DTO — safe because SQLite `AUTOINCREMENT` rowids start at 1, and `AppendAsync` ignores whatever Id it's handed on the way in regardless. Judged not worth a second type's duplication for a record where every other field is identical between the two conceptual "shapes."
- **Concurrency model, documented in `SqliteHistoryStore`'s class doc comment**: one long-lived `SqliteConnection`, opened lazily on first real call (not in the constructor, so a bad path never makes construction itself throw) and held for the store's lifetime, with ALL access serialized through a single `SemaphoreSlim(1,1)` rather than a connection-per-call design. Chosen because this store will eventually be called both from a hot, fire-and-forget dictation-completion path (§3.6, a background/worker context, not yet built) and a UI search box (§3.10/item 6, not yet built) potentially concurrently — a semaphore around one connection gives simple, correct mutual exclusion without needing to handle SQLite's `SQLITE_BUSY`/retry-loop concerns a naive connection-per-call-under-write-contention design would otherwise require. `PRAGMA journal_mode=WAL` set defensively at initialization even though the in-process semaphore already prevents concurrent access from this process today.
- **`AppendAsync` proven non-throwing on a real simulated write failure**, mirroring `ConfigService`/`DictionaryService`'s established "simulate a bad path, confirm log-and-continue" test shape: the test points the store's db path at a location whose parent path segment is an existing FILE (not a directory), so `Directory.CreateDirectory`/`SqliteConnection.OpenAsync` cannot succeed — a deterministic, cross-platform-consistent failure that doesn't depend on OS-specific ACL manipulation. Confirmed `AppendAsync` returns the `-1` sentinel and logs an error rather than throwing, and that `SearchAsync`/`PurgeOlderThanAsync`/`PanicWipeAsync` all degrade the same non-throwing way once initialization has failed.
- **Deliberately does NOT mirror `ConfigService`/`DictionaryService`'s hot-reload shape** — no `FileSystemWatcher`, no debounce `Timer`, no `Current`/change-event pair. `IHistoryStore` is an append/query store with no external file for a human to hand-edit, so forcing that shape on would have been pattern-matching without a real underlying need, per this work item's own explicit instruction not to force-fit.
- 7 new tests in `tests/Soneto.Core.Tests/History/SqliteHistoryStoreTests.cs`, all against real temp-file SQLite databases (no mocks): FTS5-for-real via a genuine match/non-match pair; full round-trip of every `HistoryEntry` field including a non-empty `RulesFired` (`AppliedRule` round-trips through `System.Text.Json`'s default positional-record handling with no custom converter needed); null/empty/whitespace query returning entries newest-first without requiring any matching content; `limit`/`offset` paging in both the browse-all and FTS-match code paths; `PurgeOlderThanAsync` deleting only entries older than the cutoff with a correctly returned deleted-count; `PanicWipeAsync` emptying the store completely including the FTS-phantom-hit check; and the non-throwing simulated-write-failure proof.
- `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 452/452 (445 + 7 new), `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 pre-existing hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 52/52 (unchanged) — matches the pre-item-1 baseline exactly aside from the 7 new tests.
- **Addendum (2026-09-02) — independent verification found one genuine test-coverage gap in item 1, closed same day.** The original `AppendAsync_never_throws_on_a_simulated_write_failure_and_returns_the_sentinel` test only proved INIT-time failure handling — it points the db path at a location whose parent segment is an existing file, so `EnsureInitializedAsync` itself fails and sets `_initFailed`, which makes `AppendAsync` return `-1` via its very first check WITHOUT ever entering its own method body. That test never exercised `AppendAsync`'s own `catch (SqliteException or IOException)` clause wrapping the actual `INSERT`/`last_insert_rowid()` calls — a genuinely different failure mode (a mid-session disk-full/locked/deleted database out from under an already-open, already-successfully-initialized connection, not "the store could never open a connection at all"), confirmed by the fact the old test's log-message assertion checks for `"Failed to initialize history store"`, never the distinct `"Failed to append history entry to {DbPath}; dropping this entry"` message `AppendAsync`'s own catch clause logs. **Closed with a new test, `AppendAsync_never_throws_on_a_write_time_failure_after_successful_initialization`**: lets a real `SqliteHistoryStore` construct and initialize successfully (one genuine append succeeds first), then a second, independent, short-lived `SqliteConnection` to the same db file issues `DROP TABLE history;` and disposes — since the store's own connection is never mid-transaction between calls, this second connection acquires and releases its lock immediately with no contention. The store's next `AppendAsync` call then hits a genuine `SqliteException` ("no such table: history") from inside its own `INSERT`, caught and logged with the distinct message, returning `-1` rather than throwing. **Why this approach over the two alternatives the gap-fix task suggested:** simulating `SQLITE_BUSY` lock contention (a second connection holding an uncommitted `BEGIN IMMEDIATE` write transaction) was tried first and empirically measured at ~30 seconds per attempt, since `Microsoft.Data.Sqlite` sets a 30-second default busy-timeout on every connection it opens (confirmed via a standalone scratch repro, not assumed) — impractically slow for a unit test; making the db file read-only via `File.SetAttributes` was also considered but judged unreliable, since an already-open write handle on Windows is not guaranteed to be affected by an attribute change made after the fact. The table-drop approach produces the exact target exception type synchronously (~2ms observed) with no OS-specific behavior to rely on. Also added, as a low-cost bonus: after recreating the `history` table's schema via a third short-lived connection (mirroring the store's own `CREATE TABLE IF NOT EXISTS` DDL verbatim), a subsequent append/search on the SAME store instance succeeds — confirming one transient write-time failure doesn't permanently wedge the store. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 453/453 (452 + 1 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).
- **Addendum (2026-09-02) — code review found one BLOCKING dispose-race bug and one should-fix in item 1's `SqliteHistoryStore`, both closed same day.**
  - **Blocking bug:** none of the four public methods (`AppendAsync`/`SearchAsync`/`PurgeOlderThanAsync`/`PanicWipeAsync`), nor `EnsureInitializedAsync`'s already-initialized fast path, ever checked `_disposed`. A call made after `await store.DisposeAsync()` completed either hit an accidental `ObjectDisposedException` thrown by the already-disposed `_gate` `SemaphoreSlim` itself — uncaught by the `catch (SqliteException or IOException)` clauses since it happens before the method's own try block even starts, silently violating every method's documented "never throws" contract — or, in a narrower race (a caller's `_gate.WaitAsync` racing `DisposeAsync`'s own connection-nulling/gate-disposal sequence), a raw uncaught `NullReferenceException` from dereferencing the already-nulled `_connection`. **Fix genuinely mirrors `ConfigService.Dispose()`'s own established pattern** (`src/Soneto.Core/Configuration/ConfigService.cs`), not just superficially: `_disposed` is now flipped to `true` UNDER THE SAME gate that guards the resource being disposed (`_gate` for `_connection`), and a new `ThrowIfDisposed()` helper is called both immediately before AND immediately after acquiring that gate in every public method (plus `EnsureInitializedAsync`'s fast path, which previously bypassed the gate entirely once initialized once — exactly the path that made the bug 100% reproducible). This closes the race the same way `ConfigService` closes it: no caller can ever observe a partially-disposed state, because the flip and the check happen under the identical synchronization primitive. `IHistoryStore.cs`'s own doc comment now explicitly documents this as the normal `IAsyncDisposable` misuse-after-dispose contract (throws `ObjectDisposedException`), distinct from the narrower "never throws on environmental I/O/SQLite failure" promise the interface already made. **Neither `SemaphoreSlim` gate (`_gate`/the new `_readGate`, see below) is ever disposed** — `DisposeAsync`'s own doc comment explains why: disposing them would let the runtime's own accidental `ObjectDisposedException` (thrown by an in-flight `WaitAsync` call) race against the deliberate, checked one `ThrowIfDisposed()` raises, defeating the whole point of making post-dispose behavior consistent — the same trade-off `ConfigService.Dispose()` already makes by never disposing its own plain-`object` `_gate` lock. New test `Every_public_method_throws_ObjectDisposedException_after_DisposeAsync_not_NullReferenceException_or_a_raw_SqliteException` in `SqliteHistoryStoreTests.cs` proves all four public methods throw `ObjectDisposedException` consistently after a real `DisposeAsync()` call (not `NullReferenceException`/a raw `SqliteException`), and that a second `DisposeAsync()` call remains the safe no-op `IAsyncDisposable` idempotency requires.
  - **Should-fix:** `SearchAsync` previously shared the single write `_gate`/`_connection` with `AppendAsync`/`PurgeOlderThanAsync`/`PanicWipeAsync`, so a slow search (a large `limit`, or the `LIKE` fallback path) would block `AppendAsync`'s hot, fire-and-forget dictation-completion-path call for the search's full duration — directly contradicting §3.5's "must never add latency to the injection path" requirement, and defeating the whole point of the `PRAGMA journal_mode=WAL` already set at initialization (WAL's actual reader/writer non-blocking benefit only applies across separate connections; one shared connection behind one semaphore serializes everything regardless). Since nothing yet calls this store concurrently (item 2's `SessionController.DictationCompleted` and item 6's UI search box don't exist yet), fixed now with a real architectural change rather than deferred: `SearchAsync` now uses its own dedicated read-only `SqliteConnection` (opened with `SqliteOpenMode.ReadOnly`, so this path can never itself mutate the database even by accident) and its own separate `_readGate` semaphore — kept (rather than no gate at all) because a single connection still isn't safe for genuinely concurrent command execution, but since it's a SEPARATE gate from the write path's `_gate`, concurrent searches now only ever serialize against each other, never against appends/purges/wipes. `SqliteHistoryStore`'s class doc comment rewritten to document both the write-side and read-side concurrency models and the reasoning behind the split.
  - `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 454/454 (453 + 1 new), `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 pre-existing hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 52/52 (unchanged).
- **Addendum (2026-09-02) — a skeptical final re-review of the dispose-race fix traced the actual gate-acquisition interleavings by hand rather than trusting the doc comments, and confirmed it holds.** `DisposeAsync` acquires both gates (write then read, one consistent order — no deadlock risk since no code path acquires them in the reverse order) and only flips `_disposed`/disposes either connection inside that fully-nested critical section, so a caller that has already passed its own `ThrowIfDisposed()` check and is waiting on (or holding) a gate can never have its connection yanked out mid-operation, regardless of which side wins the race to acquire first — verified by manually walking both orderings, not just asserted. Two small non-blocking gaps the re-review surfaced were closed the same day: (1) the "same trade-off `ConfigService` makes" framing for not disposing the semaphores overstated the analogy — `ConfigService`'s own `_gate` is a plain `lock`-only `object` with no `Dispose` method at all, so it never actually faced this trade-off; the doc comment was reworded to justify the choice on its own merits instead of via a comparison that didn't hold up under a second look. (2) The one dispose-safety test (`Every_public_method_throws_ObjectDisposedException_after_DisposeAsync_...`) was purely SEQUENTIAL — dispose completes fully, then each method is called — and never actually exercised the race the fix exists to close. **Closed with a new test, `Concurrent_operations_racing_DisposeAsync_never_throw_anything_but_ObjectDisposedException`**: across 25 iterations, each against a fresh store instance, fires 8 concurrent `AppendAsync`/`SearchAsync`/`PurgeOlderThanAsync` calls via `Task.Run` alongside a concurrent `DisposeAsync()` call, then asserts every one of the 25 batches' tasks settles on either success or a clean `ObjectDisposedException` — never a `NullReferenceException` or any other uncaught exception, which is exactly the failure signature a regression in the gate-ordering fix would reintroduce. `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 455/455 (454 + 1 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52). **Phase 3 item 1 (`IHistoryStore` + SQLite/FTS5) is done.**

## 2026-09-02 (Phase 3 item 0)

- **Phase 3 item 0 (extract `Soneto.Daemon`'s real end-to-end composition logic into a shared `Soneto.Composition` library), green — `Docs/soneto-implementation-plan-phase3.md` §3.16 item 0 done.** New project `src/Soneto.Composition/Soneto.Composition.csproj` (`net10.0;net10.0-windows`, mirroring `Soneto.Daemon.csproj`'s TFM shape and `WINDOWS`-symbol-fix comment verbatim; references `Soneto.Core`/`Soneto.Platform.Linux` unconditionally and `Soneto.Platform.Windows` only under `net10.0-windows`), added to `soneto.slnx` under `/src/`. Its single public class, `Soneto.Composition.DaemonComposition`, now holds everything that used to be ~900 lines of `Soneto.Daemon/Program.cs`: `CreateConfigService`/`CreateDictionaryService` (plain factory methods), `LoadAndStartWatchingConfigAsync`/`LoadAndStartWatchingDictionaryAsync` (load + log + subscribe-after-load + `StartWatching`, byte-identical log lines/ordering to before), `CreatePlatformHotkeySourceAndTextInjector` (the `#if WINDOWS`/`#else` per-platform selection, previously inline in `Main`), and the full `BuildAndStartSessionControllerAsync`/`BuildPostProcessors`/`FindDevModelDirWalkingUp` chain (moved verbatim, doc comments intact).
- **Design decision: plain static factory methods taking `ILoggerFactory`, not `IServiceCollection` DI registration.** `Soneto.Daemon` today builds its services through a `Microsoft.Extensions.Hosting` `HostApplicationBuilder`'s DI container; a future `Soneto.App` (Avalonia, item 4 onward) is not guaranteed to use the same container shape. Rather than force a second caller to adopt `Microsoft.Extensions.DependencyInjection` just to satisfy this helper, every `DaemonComposition` method takes an `ILoggerFactory` (a shape both a console host and a hand-rolled Avalonia composition root can trivially produce) and returns constructed instances directly — `Soneto.Daemon`'s former `builder.Services.AddSingleton<IConfigService>(...)`/`AddSingleton<IDictionaryService>(...)` registrations became direct `DaemonComposition.CreateConfigService(loggerFactory, configPath)`/`CreateDictionaryService(...)` calls made right after `host.Build()`, which is behaviorally identical (same `ILogger<T>` category names, same single-instance-for-process-lifetime shape).
- **The config-hot-reload extension seam this item's "done when" bar calls for:** `LoadAndStartWatchingConfigAsync` takes an optional `Action<ConfigChangedEventArgs> onConfigChanged`, invoked after the shared helper's own "Config hot-reloaded..." log line — `Soneto.Daemon` passes its Serilog `levelSwitch` live-update logic (`--verbose` override + `logging.level` parse) through this delegate rather than the shared helper hardcoding Serilog-specific behavior, since Serilog bootstrap is inherently per-executable and stays in each executable's own `Program.cs`. The dictionary-hot-reload reaction (the "daemon restart required, `PostProcessorChain` built once" `LogWarning`) has no equivalent seam — it's not daemon-specific, so it's hardcoded once in the shared helper, applying identically to any future caller.
- **Zero behavior change to `Soneto.Daemon`, confirmed multiple ways.** `Soneto.Daemon/Program.cs` keeps its CLI flag branches (`--transcribe`/`--record`/`--capture-demo`/`--vad-demo`/`--watch-hotkey`/`--inject`/`--hold-shift-ms`), Serilog bootstrap, and `Main`'s own top-level structure untouched — only the composition block (DI registration → factory calls, inline config/dictionary load/watch → `DaemonComposition` calls, inline `#if WINDOWS` platform selection + `BuildAndStartSessionControllerAsync` call → `DaemonComposition` calls) changed, and the two CLI-only helpers (`RunTranscribeCommandCoreAsync`/`TryBonusTranscribeAsync`) that used a local `FindDevModelDirWalkingUp` copy now call the same logic via `DaemonComposition.FindDevModelDirWalkingUp` (kept `public`, not `internal`, specifically so these CLI-harness-only call sites — which stay in `Program.cs` per this item's scope — can still reach it). `dotnet build soneto.slnx` — 0 warnings/errors, both TFMs, `Soneto.Composition` built for both. `dotnet test soneto.slnx` — 445/445 `Soneto.Core.Tests`, 99 `Soneto.Platform.Windows.Tests` (97 passed + 2 hardware-gated skips), 52/52 `Soneto.Platform.Linux.Tests` — identical to the pre-item-0 baseline, no test changes needed since this was a pure extraction with no new public surface in `Soneto.Core`. A live `dotnet run --project src/Soneto.Daemon -f net10.0` startup showed the identical "Config loaded from...”/"Dictionary loaded from... entries”/"SessionController started..." log-line shape as before (the run faulted on `LinuxHotkeySource` finding no `/dev/input` — expected, pre-existing behavior for the plain `net10.0` Linux-targeted build on a Windows OS box, unrelated to this refactor); a real `--transcribe <wav>` run against a real (non-16kHz, correctly rejected) test WAV confirmed the CLI-only path still resolves the model via the moved `FindDevModelDirWalkingUp` and produces the same error message as before.
- Docs updated per this project's standing convention: `Docs/soneto-implementation-plan-phase3.md` §3.16 item 0 row marked ✅; `Docs/PROJECT-MEMORY.md` gained a new "Phase 3 — Avalonia Shell" current-phase section; `Docs/ARCHITECTURE.md`'s solution-layout tree and prose now mention `Soneto.Composition`'s existence/purpose.
- **Independent test-runner verification went beyond code-reading: a real live smoke test against the actual ASR model on this machine** proved the full pipeline starts end-to-end identically to before (config/dictionary load → ASR cold load → VAD load → hotkey source start → "SessionController started: real end-to-end dictation is live"), plus a deliberate negative-path run (the Linux build on Windows) confirmed the daemon stays alive on a platform-component fault rather than crashing, matching the "never exits on a recoverable error" contract carried into the new shared helper. **Code review found no blocking issues.** One should-fix applied: `Docs/soneto-implementation-plan-phase3.md` §3.3/§3.4 had originally deferred the "shared library vs. `Soneto.App`-nested-folder" choice to item 4 and sketched the helper at `Soneto.App/Composition/` — both sections updated to name `Soneto.Composition` as the actual top-level library this item built, so the plan and the as-built code agree. One nit (deriving `BuildAndStartSessionControllerAsync`'s logger internally instead of taking a caller-supplied `ILogger`) was deliberately NOT applied — that parameter is what preserves the exact pre-refactor log-CATEGORY (`ILogger<Program>`) for critical/info startup messages, and changing it would itself be an observable behavior change, contradicting this item's own zero-diff bar; left as a documented open question for whenever `Soneto.App` exists and has its own opinion on logger categories.

## 2026-09-02

- **Phase 2 item 10 (seed dictionary + full `Program.cs` daemon wiring), green — Phase 2 complete (all 11 build-order items 0-10 done).** `src/Soneto.Core/Dictionary/Resources/seed-dictionary.json` (new embedded resource, loaded via a new `SeedDictionary` static class mirroring `WordFrequencyList`'s established resource-loading pattern) ships build-plan.md §6.3's literal 24-term vocabulary list (`webMethods`, `Integration Server`, `Trading Networks`, ..., `PKCS#12`, `JKS`) as `VocabularyTerm` entries, plus the 4 built-in EN/RO `SpokenCommand` phrases folded in as their own seed entries, per §2.10's own instruction.
- **A real `DictionaryService` behavior change, not a purely additive one: first-run now writes and loads the seed dictionary, superseding item 9's deliberately-deferred "start empty, write nothing" behavior.** Now that a seed dictionary exists, `LoadAsync`'s missing-file path writes the embedded seed JSON to `DictionaryPath` (falling back to using it purely in-memory if the write itself fails — e.g. a permission-denied directory — mirroring `ConfigService`'s own fallback) and then runs it through the SAME parse/validate pipeline as any other load, so the seed content is genuinely round-tripped through real per-entry deserialization and all three §2.7 validation rules, not just trusted as valid JSON by construction. First-run loads still do not raise `DictionaryChanged` (an `isFirstRun` flag now threads through to the existing `SetCurrent(config, raiseEvent: ...)` call), preserving the exact same "no reaction to react to on the very first load" contract `ConfigService` itself established. The one item-9 test that asserted the old write-nothing behavior was updated in place (not deleted) to assert the new one.
- **Design decision, made explicitly and documented in three places (the new `SeedDictionary` class's own doc comment, this changelog, and the plan doc): the 4 seed `SpokenCommand` entries deliberately share their `Phrase`s with `SpokenCommandsExtensionProcessor.BuiltInDefaults`, using distinct `seed.spoken-command.*` Ids (not the hardcoded `builtin.spoken-command.*` ones).** Per that processor's own already-documented collision policy (a `dictionary.json` entry sharing a built-in's phrase silently wins, keyed on `Phrase` not `Id`), these 4 seed entries now take over `AppliedRule` provenance for these 4 structural commands the moment the seed dictionary loads — functionally identical output (same phrase, same emitted control characters), just a different `Id` in any future rule-fired diff. Judged correct, not a bug to avoid: the seed dictionary becoming the "real," user-editable source of truth for these 4 commands is exactly what §2.10 itself asks for. `BuiltInDefaults` is left completely unmodified as the zero-dependency fallback for the now-rarer case where `dictionary.json` can't be read or written at all.
- **`PostProcessConfig` gains 3 new toggles, all defaulting to `true` (matching every existing toggle's default): `DictionaryEngine`, `RegexRules`, `FillerWordStripping`.** `DictionaryEngine`/`RegexRules` are deliberately TWO SEPARATE switches rather than one shared "dictionary" toggle — the trie-based correction-pair workhorse's no-cascade guarantee and the regex pass's deliberately-cascading power-user escape hatch are functionally distinct enough (different risk profiles, per each processor's own doc comment) that a user debugging one shouldn't have to also disable the other to isolate which pass is responsible. `FillerWordStripping` gets its own toggle because (per item 9's own design note) `FillerWordStripper` has no `dictionary.json`-backed entry type to gate it any other way.
- **`Soneto.Daemon/Program.cs` now constructs a real `DictionaryService`** — sibling DI registration/load/watch wiring to the existing `IConfigService` block (same shape: informational log line on load, subscribe to the changed event only after the initial load completes) — and threads its loaded `Current.Entries` into `BuildPostProcessors`, so `DictionaryEngineProcessor`/`RegexRuleProcessor`/`SpokenCommandsExtensionProcessor` are now constructed from the REAL loaded dictionary instead of an empty list (`SpokenCommandsExtensionProcessor`) or not being constructed at all (`DictionaryEngineProcessor`/`RegexRuleProcessor`).
- **Honest, deliberate limitation — `SessionController.cs` was read and intentionally NOT changed, per plan §1.16/§2.7's own explicit instruction to treat "this needs a `SessionController` change" as a signal something is wrong, not a challenge to solve.** `PostProcessorChain` is still built exactly once at daemon startup, from a snapshot of both `config.PostProcess` and the loaded dictionary entries — a pre-existing gap for `config.PostProcess` toggles since item 9 (Phase 1 never rebuilt the chain either), not newly introduced by this item. A hot-reloaded `dictionary.json` is still validated/logged by `DictionaryService`, but its `DictionaryChanged` handler in `Program.cs` now logs a clear `LogWarning` explaining a daemon restart is required for the change to take effect in the active session — a named, documented limitation (same honesty standard as Phase 1's items 11/12 gaps), not a silently-broken promise.
- **End-to-end test added to `PostProcessorChainTests.cs`** (§2.11's closing bullet): all seven processors, corrected order 10→30→40→50→60→70→90, one representative transcript exercising diacritic normalization + a real dictionary correction + a cascading regex rule + a spoken command + comma-bounded filler-word stripping + the trailing-space processor running last — hand-traced stage-by-stage through the real regex/casing/cleanup logic before running it, and correct on the first try. A new `SeedDictionaryTests.cs` (6 tests) round-trips the embedded seed dictionary through the real `DictionaryService` load pipeline, confirming zero rejected entries and the expected vocabulary/spoken-command content.
- `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 443/443 (436 + 7 new), `Soneto.Platform.Windows.Tests` 99 (97 passed + 2 hardware-gated skips, unchanged), `Soneto.Platform.Linux.Tests` 52/52 (unchanged).
- **Addendum — independent verification found the 443/443 above was functionally correct but had two genuine test-COVERAGE gaps, both closed same day.** Gap 1: the §2.11 full-chain test above exercised all 7 processors' ORDERING correctly but used a synthetic, hand-built `CorrectionPair`/`RegexRule` pair rather than the real `seed-dictionary.json` — nothing in the suite proved the actual shipped seed content (a `VocabularyTerm`, not a `CorrectionPair`; zero `RegexRule`s) composes correctly through the real chain. Fixed with a new `PostProcessorChainTests.FullChain_RealSeedDictionary_MultiFeatureTranscript_ProducesCorrectOutput`, which loads the seed dictionary through a real `DictionaryService` first-run, builds the exact same 7-processor chain `Program.cs`'s `BuildPostProcessors` builds, and proves a real seed `VocabularyTerm` casing correction ("webmethods" → "webMethods") and a real seed `SpokenCommand` ("new paragraph" → "\n\n") both come out correct end-to-end (no regex assertion added, since the real seed file has none to exercise). Gap 2: the seed dictionary's 4 `SpokenCommand` entries deliberately collide by phrase with `SpokenCommandsExtensionProcessor.BuiltInDefaults` and silently take over `AppliedRule` provenance on load (documented above) — but nothing in the suite proved the seed file's actual `emits` values still produce correct output, so a future accidental edit to `seed-dictionary.json` breaking one of the 4 phrases/emits would have gone completely undetected by CI. Fixed with a new `SeedDictionaryTests.SeedDictionary_SpokenCommands_ProduceCorrectOutput_ThroughARealProcessor_AndOverrideBuiltInProvenance`, which loads the real seed dictionary, builds a real `SpokenCommandsExtensionProcessor` from it, and asserts both the correct emitted text AND the `AppliedRule.Rule` provenance (`seed.spoken-command.*`, not `builtin.spoken-command.*`) for all 4 phrases. Re-ran `dotnet build soneto.slnx` (0 warnings/errors) and `dotnet test soneto.slnx` (`Soneto.Core.Tests` 445/445 = 443 + 2 new; `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged) — all green.
- **Final code review (post gap-fix) found no blocking or should-fix issues — clean pass, closing out Phase 2.** Specifically checked and confirmed sound: the new real-seed-dictionary tests are genuinely load-bearing on the shipped file content (would fail if `seed-dictionary.json` were broken), not coincidental passes; the spoken-command test's inputs genuinely exercise `SpokenCommandsExtensionProcessor`'s real punctuation-boundary regex, not just an end-of-string shortcut; the first-run seed-write path's failure handling never throws and never bypasses validation (the written-or-in-memory-fallback JSON always runs through the same parse/per-entry/whole-file pipeline as any other load); `DictionaryEngine`/`RegexRules` toggle independence is safe (no shared mutable state between the two processors); `Program.cs` sequences `LoadAsync()` fully to completion before `StartWatching()`, so there's no TOCTOU window where the watcher could race its own first-run write; and — the phase-level bar from §1.16 — `SessionController.cs` has zero references to anything `Dictionary`-namespaced across all 11 build-order items (0-10), confirming Phase 2 was genuinely purely additive end to end. **Phase 2 — Dictionary Engine — is now complete.**

- **Phase 2 item 9 (`DictionaryPaths` + `DictionaryService` hot-reload), green.** `src/Soneto.Core/Dictionary/DictionaryPaths.cs` mirrors `ConfigPaths.Resolve()` verbatim (explicit-override-then-OS-default, no platform project reference). `src/Soneto.Core/Dictionary/DictionaryService.cs` mirrors `ConfigService`'s established, twice-reviewed shape closely — same 500ms-debounce `FileSystemWatcher`+`Timer` pattern, same `_disposed`-flag-flipped-under-the-same-lock race-closing pattern, same "never throws, keep the previous good state on any failure" contract — as a sibling service rather than generalizing `ConfigService` to a dual-file watcher, per the plan's own explicit recommendation. Closes item 1's noted per-entry-JSON-error-isolation gap for real: `DictionaryDocument`'s whole-document deserialize is all-or-nothing, so `DictionaryService` instead parses `entries` as raw `JsonElement`s and deserializes each one individually inside its own try/catch — a single malformed entry (bad `type` discriminator, missing required field) is logged and skipped by index (and Id, if recoverable) without failing the rest of the file. New `src/Soneto.Core/Dictionary/DictionaryConfig.cs` adds the runtime, validated-entries shape (`DictionaryConfig`/`RejectedDictionaryEntry`) that `DictionaryDocument`'s own doc comment called for — a plain data holder, not a service, carrying both the surviving entries and per-rejection diagnostics.
- **All three §2.7 validation rules implemented exactly as specified, each at the right granularity.** An enabled `RegexRule.Pattern` that fails to compile (`RegexParseException`) rejects that ONE entry, logged clearly, rest of the file still applies. An empty/whitespace-only `CorrectionPair.From`/`SpokenCommand.Phrase` rejects that ONE entry the same way. Duplicate `DictionaryEntry.Id` values anywhere across the file reject the WHOLE file — the previous good `DictionaryConfig` is retained wholesale and no `DictionaryChanged` event fires — since ambiguous `Id`s would corrupt `AppliedRule` correlation in a way no partial-file fix could repair. Item 8's `DictionaryCollisionWarnings.Check` now has its real call site, firing on every successful load/reload against the full validated entry set.
- **Design decision: no `Rebuild()` method added to the four dictionary-adjacent processors.** §2.7's text frames a live `Rebuild(IReadOnlyList<DictionaryEntry>)` method as a "recommendation," not a hard requirement, and the actual `PostProcessorChain`-swapping wiring into `Program.cs` is explicitly item 10's job per the plan's own build-order table — adding `Rebuild()` now, with no real consumer yet, would have been scope creep into item 10's work. Instead, live-rebuild-without-a-daemon-restart is demonstrated at exactly the level this item owns: a test writes a changed `dictionary.json` to disk, waits for the debounced `DictionaryChanged` event, then constructs a FRESH `DictionaryEngineProcessor` from the newly reloaded `Current` entries and proves its output genuinely differs (correctly) from one built earlier from the old entries — no daemon restart anywhere in the test, proving the full "file changes on disk → debounced reload → new entries observable → fresh processors behave differently" round-trip this item's own "done when" bar asks for.
- **Deliberate first-run divergence from `ConfigService`, called out explicitly rather than left implicit:** a missing `dictionary.json` does NOT get a defaults file written to disk the way a missing `config.json` does — `Current` just starts at `DictionaryConfig.Empty` in memory. There's no seed dictionary to write yet (that's item 10's job), and writing an empty file now would only be something item 10's seed-dictionary write would immediately have to detect-and-skip past.
- `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 436/436 (424 + 12 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged.
- **Independent test-runner verification confirmed all 10 of §2.7/§2.11's pass criteria directly**, not by re-running the implementer's own suite: the per-entry-isolation test genuinely sandwiches a bad entry between two good ones (not "the whole file is one bad entry"); the duplicate-Id test confirms the previous good `Current` is retained, not just that the load returns `false`; the debounce test drives 4 rapid writes through the REAL `FileSystemWatcher` path and confirms exactly 1 reload; the live-rebuild test goes through a real file write + debounced reload, not a direct `LoadAsync()` shortcut. **Code review found no blocking issues; 2 should-fix items applied same day:** (1) `IDictionaryService.LoadAsync`'s doc comment wrongly claimed a first-run write-then-load — copy-pasted from `IConfigService` and never updated for this item's deliberately different (write-nothing) first-run behavior — corrected to match the class's own already-accurate doc comment; (2) `DictionaryService`'s file-read path caught only `IOException`, not `UnauthorizedAccessException`, contradicting its own interface's explicit "never throws on an unreadable file" contract — and the SAME gap was found to pre-exist in `ConfigService.cs`'s read path too (not a new regression, a latent shared one), so both were fixed together (`catch (Exception ex) when (ex is IOException or UnauthorizedAccessException)`). Re-verified 436/436 green, 0 warnings/errors, after both fixes.

- **Phase 2 item 8 (`WordFrequencyList` + collision-warning logging), green.** `src/Soneto.Core/Dictionary/WordFrequencyList.cs` + `DictionaryCollisionWarnings.cs` — the authoring-time safety net from build-plan.md §6.2 rule 6: a hand-curated, honestly-scoped (not exhaustive, per the plan's own explicit allowance) embedded EN (483 words) + RO (362 words) common-word list, and a standalone advisory check that logs a warning when a single-word `CorrectionPair`/`VocabularyTerm` pattern IS itself an ordinary common word (e.g. `"cloud"`) — the exact authoring-time risk that item 3's runtime full-token-boundary rule guards against at match time. Deliberately not wired into any real flow yet — `DictionaryService` (item 9) doesn't exist — built as a trivial one-line-call-site utility (`Check(entries, logger)`, no state to manage, `WordFrequencyList.Instance` a safe CLR-guaranteed-once static-initializer singleton) specifically so item 9 can wire it in without friction.
- **Independent verification stress-tested edge cases beyond the implementer's own tests**: confirmed the multi-word carve-out holds even when a token within a multi-word pattern is itself a common word; explored a hyphenated-pattern edge case and confirmed the "safe with current lists" reasoning is correct but conditional (a hyphenated pattern COULD in principle collide if the whole hyphenated string were ever added to the word list verbatim — not the case today, but a real edge worth being aware of for future list growth); confirmed case-insensitivity end-to-end through the real call path, not just the lookup in isolation; confirmed disabled entries and non-`CorrectionPair`/`VocabularyTerm` types are structurally excluded, not just coincidentally silent.
- **Code review found no blocking issues, two small fixes applied**: removed 8 duplicate lines the word-list source files had accumulated (functionally inert since the lookup dedupes on load, but undercut the "carefully curated" framing the class's own doc comment gives these files); softened an unqualified "never throws" doc claim to note the one theoretical (build-time-realistically-impossible) exception path, so the documentation is literally accurate rather than a slight overclaim.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 424/424 (403 + 21 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 7 (`FillerWordStripper`), green — the last of the four dictionary-adjacent `IPostProcessor`s.** `src/Soneto.Core/Dictionary/FillerWordStripper.cs` (order 70) strips free-standing EN/RO filler tokens (`um`, `uh`, `ăăă`, `păi`) with the same Unicode-letter/digit full-token-boundary discipline used elsewhere in Phase 2 — confirmed against fresh adversarial real words (`plumber`, `Wuhan`, the Romanian word `împăiat`), none corrupted. Deliberately NOT backed by `dictionary.json`/`DictionaryEntry` at all (no entry type exists for filler words, per the plan's own framing — a small, low-risk hardcoded list, clearly documented as the reason this processor diverges from items 4/5/6's `IEnumerable<DictionaryEntry>` constructor convention) — a simple `bool enabled` toggle plus an optional custom-list override that REPLACES (not merges with) the defaults.
- **Inherited and correctly handled the same order-70-runs-after-order-30 cleanup responsibility item 6 established a precedent for**: since this runs after `WhitespaceCleanerProcessor`, removing a filler word from mid-sentence needs its own local cleanup of the resulting gap (stray double-spaces, comma runs, leading/trailing punctuation) rather than relying on downstream cleanup that already happened. Independent verification surfaced two genuine, narrow edge cases in the cleanup scope — a single comma on only one side of a removed filler word, and a filler word immediately preceding terminal sentence punctuation — both explicitly disclaimed in the class's own doc comment as deliberate scope limits, not silent gaps.
- **Code review applied real judgment on which of the two gaps was worth closing now**: the asymmetric-single-comma case was judged genuinely low-frequency (real ASR punctuation models predominantly produce either no comma or a comma on both sides of a hesitation, since a spoken pause is symmetric) and left documented-but-unfixed; the terminal-punctuation case was judged plausible enough to fix now specifically BECAUSE this is a push-to-talk app — a user trailing off with "um" right before releasing the key, followed by an appended terminal period, is a realistic pattern for Soneto's own usage shape in particular, more so than for a continuously-listening dictation tool. Fixed with one more narrowly-scoped cleanup regex; the old test that had pinned the previous (buggy) output as expected behavior was correctly updated to assert the new correct output, not left stale.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 403/403 (358 + 45 new across both verification passes and the fix), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 6 (`SpokenCommandsExtensionProcessor`), green — retires Phase 1's `SpokenCommandsProcessor` in the same session, per §2.6's plan.** `src/Soneto.Core/Dictionary/SpokenCommandsExtensionProcessor.cs` (order 60, moved from Phase 1's order 20) replaces the fixed EN/RO table with a user-extensible one, migrating the 4 built-in phrases as bundled defaults so spoken commands never went non-functional in the gap between the old class's deletion and the new one's construction. Deliberately does NOT reuse the shared `AhoCorasickAutomaton` (items 3/5) — that class's boundary rule only guards letter/digit adjacency, weaker than what spoken commands actually need (item 8's own validated punctuation/utterance-boundary rule) — instead porting item 8's exact regex-based matching approach verbatim, preserving the real bug fix that rule represents (the "my new line of business" non-mangling requirement).
- **Caught and fixed a real, non-obvious second-order regression the reorder itself introduced.** Phase 1's `WhitespaceCleanerProcessor` (order 30) was specifically built to run AFTER spoken commands so it could preserve their freshly-emitted `\n`/`\n\n` while still cleaning surrounding horizontal whitespace. With spoken commands now at order 60 — AFTER `WhitespaceCleaner`, not before — a spoken command's emitted newline would never get that cleanup at all, leaving stray whitespace (e.g. `"okay, \n\n, next item"` instead of `"okay,\n\n, next item"`). Found by hand-tracing a concrete example before it could become a real regression, fixed with a small, narrowly-scoped local cleanup inside the new processor itself (stripping only horizontal whitespace immediately adjacent to text it just emitted, not a general-purpose pass duplicating `WhitespaceCleanerProcessor`'s job). Independently re-verified with a DIFFERENT hand-traced example (a Romanian phrase, deliberately extra spacing) through the real, fully-composed chain — confirmed correct, confirmed narrowly scoped, confirmed the underlying "can 3+ newlines ever result from two commands firing in one pass" concern is structurally impossible given the boundary rule's zero-width lookaround can never consume the separating punctuation between two matches.
- **Code review found no blocking issues, one real should-fix applied: a silent user-vs-user phrase collision.** The merge logic correctly handled a user entry overriding a built-in, but two different user-provided commands sharing the same phrase (a likely config typo, not a legitimate power-user pattern the way overriding a built-in is) would have silently dropped one with zero diagnostic. Fixed by failing fast at construction with a clear exception naming both colliding entries — consistent with this project's established "fail loud at construction, not silently" convention (`AhoCorasickAutomaton`, item 3; `RegexRuleProcessor`, item 5) — while correctly leaving the legitimate built-in-override case as silent-and-intentional.
- `dotnet build soneto.slnx` — 0 warnings/errors, all projects/TFMs including both `Soneto.Daemon` targets (since `Program.cs`'s real chain composition needed updating). `dotnet test soneto.slnx` — `Soneto.Core.Tests` 358/358 (342 − 17 removed Phase-1 tests + 33 new, net), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 5 (`RegexRuleProcessor`), green.** `src/Soneto.Core/Dictionary/RegexRuleProcessor.cs` (order 50) — the power-user regex escape hatch, a genuinely separate code path from item 4's Aho-Corasick trie with deliberately OPPOSITE cascading semantics: regex rules apply sequentially, each rule's output feeding the next rule, unlike `DictionaryEngineProcessor`'s explicit single-pass no-cascading guarantee — an intentional asymmetry, clearly documented in both classes so a future reader diffing them doesn't mistake one for buggy. Patterns compile once at construction (not per-call), with malformed regex rejected immediately via a clear exception naming the offending rule — never deferred to first use. `AppliedRule` is populated per actual match OCCURRENCE (not per rule), correctly handling a single rule matching multiple times in one transcript, each with its own real matched text and real capture-group-substituted replacement.
- **Independent verification (14 additional tests) specifically checked for a closure-capture bug in the per-occurrence `AppliedRule` population** — a `MatchEvaluator` incorrectly reusing/capturing the same match data across multiple occurrences is a classic C# pitfall — and confirmed none exists; also independently re-proved cascading, capture-group substitution, and rule-ordering with entirely fresh examples, not just re-running the implementer's own tests.
- **Code review found one real blocking issue: no bound on regex match time, a genuine ReDoS/catastrophic-backtracking exposure.** Since `dictionary.json` is hand-edited with no backtracking-safety validation, and the whole post-processor chain runs synchronously on Phase 1's single session worker thread with no cancellation path, a single pathological pattern (e.g. nested-quantifier catastrophic backtracking) could have hung the ENTIRE daemon indefinitely — not just failed one rule — directly contradicting item 10's "daemon never hangs/exits on a recoverable error" principle, and silently so (nothing would even log until an external kill). Telling detail the reviewer flagged: this class already guarded the *parse*-failure mode (bad regex syntax) at construction time but not the *runtime-hang* failure mode, which is the more dangerous of the two since parse failures are loud and immediate while hangs are silent and indefinite. Fixed with a bounded 250ms `MatchTimeout` on every compiled pattern, a caught `RegexMatchTimeoutException` treated as a skip-this-rule-only outcome (logged clearly, with a truncated text snippet, not the full transcript), and careful commit-or-skip semantics so a mid-rule timeout can never leave a partially-applied `AppliedRule` describing a text change that didn't actually land. Proven with a real catastrophic-backtracking test (`(a+)+$` against 40 non-matching characters) confirming the daemon-equivalent call returns promptly and the rest of the chain is unaffected.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 342/342 (312 + 30 new across both verification passes), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 4 (`DictionaryEngineProcessor`), green — the first real `IPostProcessor` in Phase 2.** `src/Soneto.Core/Dictionary/DictionaryEngineProcessor.cs` (order 40) wires item 3's `AhoCorasickAutomaton` into an actual correction/casing engine: filters `DictionaryEntry`s to enabled `CorrectionPair`/`VocabularyTerm` only, builds one automaton at construction (never rebuilt per-transcript), and on `Process` implements §2.5 rule 3's case-preserving-replace decision — a replacement with explicit internal casing (both an uppercase and lowercase letter) wins verbatim regardless of input casing; otherwise the original matched span's own casing pattern (Title-Case/all-caps/lowercase) is applied to the replacement. `AppliedRule` is genuinely populated for the first time in this project (Phase 1's four processors always left it empty) — `From`/`To` carry the real matched original-text span and the actual casing-decided substitution, not the abstract rule text, the right data shape for a future rule-fired diff view. `VocabularyTerm` entries are treated as implicit self-correction pairs for casing-only correction, per §2.4's spec.
- **Caught and correctly resolved a real inconsistency in the build plan's own worked example** — `Docs/dictation-app-build-plan.md`'s literal "Webmethods at a sentence start shouldn't force lowercase" example doesn't actually exercise the casing-adoption branch of its own rule, since `webMethods` (the real seed-dictionary replacement) has explicit internal casing and therefore always wins verbatim under the very rule the example is illustrating. Independent verification confirmed this reasoning is sound (checked against the actual build-plan.md wording) and confirmed the implementer's substitute test (an all-lowercase `"teh"`→`"the"` filler-normalization pair) genuinely exercises the branch the original example structurally couldn't — the same "does the test's premise actually match its own claimed logic" scrutiny that caught a real bug in item 8's `SpokenCommandsProcessor` history, this time finding the reasoning held up rather than a bug.
- **Extensive independent verification (19 additional tests) confirmed the casing-decision table, splice-loop correctness (including start/end boundary stress), `AppliedRule` accumulation (append, not replace), and the interaction between disabled-entry filtering and the automaton's duplicate-key collision detection** — all correct. Also pinned down and confirmed as a deliberate, reasonable, now-explicitly-tested simplification (not a bug): a multi-word Title-Case original (e.g. "New Paragraph," both words capitalized) is not detected as Title-Case by the implemented single-word definition and falls back to lowercasing the replacement — word-by-word title-casing of a replacement driven by a multi-word original was never specified by the plan and would be a much larger, unscoped feature to guess at.
- **Code review found no blocking or should-fix issues** — splice-loop allocation behavior, automaton-construction-once-not-per-call, thread-safety (immutable after construction, matching item 3's own `AhoCorasickAutomaton` design), and the constructor/filtering pattern's suitability as a template for items 5-7's similarly-shaped processors were all checked and confirmed clean.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 312/312 (269 + 43 new across both verification passes), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 3 (`AhoCorasickAutomaton`), green — the plan's own "single most safety-critical piece of this whole matching engine."** `src/Soneto.Core/Dictionary/AhoCorasickAutomaton.cs`: a generic, dictionary-model-agnostic trie+failure-link matcher, matching on a fold-then-lowercase key (composing item 2's `DiacriticFolder` with case-folding) while every returned match references the ORIGINAL untouched input span — never the folded/lowercased internal key — so a later item can apply case-preserving replacement using the real original text. Implements all three of §2.5's matching rules 4-6: longest-match-first with no self-cascading (the automaton only ever scans real input, never its own hypothetical output), glue-tolerant boundaries (whitespace/hyphen runs stripped entirely from the match key on both pattern and input sides, with position-tracking reconstructing the real original span), and full-token-boundary safety (`cloud` correctly never matches inside `Cloudflare`) — built and passing FIRST, before any other logic in the class, per the plan's own explicit instruction. Along the way, correctly identified that the plan's own suggested overlap-test example (`"abcd"`/`"cdef"` against `"abcdef"`) was actually invalid under rule 6 (a continuous unbroken letter run has no internal token boundary, so the intended overlap would be silently rejected by boundary-checking before ever reaching overlap-resolution) and substituted a genuinely valid word-boundary-respecting equivalent.
- **Given the stakes, this item got an unusually thorough independent verification pass — 17 additional hand-written adversarial tests beyond the implementer's own 19**, covering Romanian-diacritic-letter word-boundary cases, start/end-of-input edge cases, multi-glue-point and multi-space position-arithmetic (hand-traced by the verifier, who caught and fixed errors in their OWN expected-value calculations rather than finding implementation bugs), and combined diacritic-form + case-fold composition. Every check confirmed correct.
- **Code review (focused on complexity/performance/API design, since correctness was already exhaustively verified) confirmed the core algorithm's amortized-linear complexity is genuinely correct** (no hidden quadratic trap in either construction or matching), and found one real should-fix: colliding patterns (two different source patterns that fold/glue-strip to the identical match key — e.g. differing only by diacritic form) produced a silently non-deterministic tie-break, since `List<T>.Sort` isn't stable and nothing tracked insertion order. Fixed by failing fast: the constructor now throws a clear `ArgumentException` naming both colliding patterns, rather than leaving which one "wins" as an unspecified implementation detail — appropriate rigor for a component this project's own review already calls "the single most safety-critical piece" of the whole engine. Two doc-comment additions also applied: an explicit stated guarantee that the class is immutable-after-construction and `Match()` is safe to call concurrently with zero synchronization (traced and confirmed true), and a complexity caveat on the failure-link output-list construction (O(k²) in pathological deep-suffix-chain pattern sets — a non-issue at the ~100-entry scale Phase 2 plans for, flagged for future reconsideration if dictionaries ever scale much larger).
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 269/269 (230 pre-item-3 + 39 new across both test files), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 2 (`DiacriticFolder`), green.** `src/Soneto.Core/Dictionary/DiacriticFolder.cs` implements §2.5 rule 2's match-only diacritic folding: `ș`/`ş` and `ț`/`ţ` fold to the same match key, `ă`/`â`/`î` fold to `a`/`a`/`i`, all case-preserving (deliberately NOT case-insensitive — that's a separate later rule/item's job) — a pure, stateless, allocation-free `char`-level function (`FoldChar`) plus a string-level convenience wrapper (`FoldForMatching`), designed with item 3's Aho-Corasick trie-walking consumption pattern in mind. Independent verification programmatically checked every folding mapping against actual Unicode codepoints (not eyeballed glyphs) and confirmed all correct — no visually-similar-but-wrong substitution, continuing this project's established codepoint-level diacritic-checking discipline.
- **Code review found no blocking issues.** One should-fix applied: documented an implicit cross-component assumption (this class assumes NFC-normalized/precomposed input, guaranteed by `UnicodeNormalizerProcessor`, order 10, running before the dictionary engine, order 40+, in the chain — a decomposed combining-mark sequence would silently fail to fold rather than throw or normalize), plus a regression test locking in that documented behavior as a checked fact rather than tribal knowledge. Mirrors the same "write down cross-component ordering assumptions explicitly" discipline the plan doc itself already calls for elsewhere.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 230/230 (207 + 23 new), `Soneto.Platform.Windows.Tests`/`Soneto.Platform.Linux.Tests` unchanged (97+2skip/52).

- **Phase 2 item 1 (data model + `dictionary.json` schema), green.** `src/Soneto.Core/Dictionary/DictionaryEntry.cs` implements the five entry types from `Docs/soneto-implementation-plan-phase2.md` §2.4 verbatim (`VocabularyTerm`, `CorrectionPair`, `RegexRule`, `SpokenCommand`, `PerAppOverride`, plus the `DictionaryEntry` base with `Id`/`Enabled`) using `System.Text.Json`'s built-in `[JsonPolymorphic]`/`[JsonDerivedType]` type-discriminated polymorphism — confirmed by review to be the right mechanism (a genuinely different problem from `CaptureModeJsonConverter`'s soft-fallback-on-bad-enum-value use case, which needed a custom converter for a different reason). `DictionaryDocument` is the JSON root (an object wrapper with `SchemaVersion` + `Entries`, chosen over a bare array for the same forward-compatibility reason `config.json` itself is an object, not an array). Independent verification did a full line-by-line comparison against the plan's literal spec and confirmed every field/default/doc-comment was carried forward faithfully, including the two "not wired up yet" hooks (`VocabularyTerm.Term`'s future hotwords-bias note, `PerAppOverride`'s data-model-only note) — no drift from the written spec.
- **Code review found no blocking issues, and empirically verified the JSON schema's failure modes by actually compiling and running a probe against the real `[JsonPolymorphic]` shape rather than trusting documentation** — confirmed an unrecognized `type` discriminator, a missing `type` field, and a missing required field all produce clear, actionable exceptions naming exactly what's wrong. Two doc-comment-only should-fix items applied (no behavior change): documented that whole-document deserialization is all-or-nothing (one bad entry fails the entire file, no partial results) — a real constraint item 9 (hot-reload/validation) needs to design around, since the plan's "never crash on a bad file" principle implies per-entry error isolation that `Deserialize<DictionaryDocument>` as built here can't give for free; and documented that `DictionaryDocument` has no structural equality (a plain class, and converting it to a record wouldn't actually fix this either, since `List<T>` doesn't override `Equals`) — a landmine flagged now rather than silently rediscovered by a future whole-document comparison test.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 207/207 (203 + 4 new), `Soneto.Platform.Windows.Tests` 97/97 + 2 pre-existing skips, `Soneto.Platform.Linux.Tests` 52/52 — all unchanged from Phase 1/item-0's baseline plus the new additive tests.

- **Phase 2 item 0 (Phase 1 reconciliation), green.** `TrailingSpaceProcessor` (item 8) renumbered from order 40 to order 90 — clears the order 40-70 range `Docs/soneto-implementation-plan-phase2.md` reserves for the new dictionary-engine processors, since trailing-space appending conceptually belongs at the very end of the chain regardless of what gets inserted in between. Pure renumbering: no test asserted the literal order number directly (`PostProcessorChain` sorts dynamically), so only doc comments needed updating, confirmed via a repo-wide grep that nothing else assumed the old value. `dotnet build` 0 warnings/errors; all three test projects unchanged from the Phase 1 baseline (203/97+2skip/52).

- **Phase 2 implementation plan written: `Docs/soneto-implementation-plan-phase2.md`.** With Phase 1's main build order (items 1-12) code-complete, this is the detailed execution plan for the dictionary engine (build-plan.md §6), mirroring `soneto-implementation-plan-phase0-1.md`'s level of detail — exact data shapes (5 entry types), matching semantics (NFC + diacritic folding, case-preserving replace, longest-match-first single-pass Aho-Corasick, full-token-boundary safety), config/hot-reload design, and an 11-item build order (items 0-10) — written before any implementation, per explicit user request. Flags two real reconciliation points found while writing it: `TrailingSpaceProcessor` (item 8) already claims order 40, colliding with the build plan's "orders 40-70" range for Phase 2 — recommends renumbering it to 90; and item 8's `SpokenCommandsProcessor` fixed EN/RO table should likely be retired and migrated into the new user-extensible dictionary rather than kept as a second, parallel spoken-command mechanism. No code written yet — this is planning only, pending user review/kickoff of the first work item.

- **Item 12 — corpus regression test + docs, green. Last item in Phase 1's main build order.** Structural wrinkle handled the same honest way as item 11's S5 dependency: the plan's "corpus regression test" is premised on spike S2's corpus existing, and S2 was deliberately deferred by explicit user decision on 2026-08-31 and has never run — no corpus, no reference transcripts, no baseline WER exists. Built the real, reusable infrastructure instead of fabricating data: `src/Soneto.Core/Evaluation/WordErrorRateCalculator.cs` (word-level Levenshtein edit-distance WER, lowercase + strip-punctuation tokenization that deliberately preserves Unicode diacritics — verified correct via independent hand-traced Levenshtein alignments, not just self-confirming tests) and `tests/Soneto.Core.Tests/CorpusRegressionTests.cs` (the `[Trait("Category","Corpus")]` harness, reusing item 3's model-resolution pattern) — which **fails loud with an actionable "run S2 first" message** when actually invoked today, rather than silently passing or asserting against an invented baseline (`S2BaselineWer` is an explicit `null` with a "do not invent a number here" comment, guarded twice: once for missing corpus data, once for a missing baseline even if data existed). Also wrote the four reference docs `Docs/SPIKE-RESULTS.md`, `ARCHITECTURE.md`, `PLATFORM-NOTES.md`, `MANUAL-TESTS.md` — the onboarding docs plan §1.15 says every future work-item session should start by reading, synthesized from real primary sources (`PROJECT-MEMORY.md`, `CHANGELOG.md`, the plan doc, actual source code) rather than paraphrased from conversation memory.
- **Independent verification spot-checked ~12 specific factual claims spread across all four new docs against their actual primary sources (real numbers, real code, real files) and found every single one accurate — no paraphrasing drift, no oversold claims.** Examples: S1's 4-thread knee finding, S1b's 58ms p95 and 1321/1213-taps-per-phase resampler correction, the `WINDOWS`-preprocessor-symbol lesson, `VK_LCONTROL`/`IsEventSimulated` specifics, the exact S4 test string and 7-app matrix (byte-for-byte against `spikes/s4-inject-win/README.md`), and confirmation that the item-9-discovered "items 6/7's own prior hardware-verification claims need re-checking" gap and item 11's Linux hardware gaps both made it into `MANUAL-TESTS.md` as real checklist items rather than being quietly dropped.
- **Code review found no blocking issues** in the WER calculator/corpus-test code — the DP table construction, backtrack logic, and resource-disposal/guard ordering were all confirmed sound. Two small should-fix items applied: documented (rather than left silently undocumented) that the tokenizer's apostrophe/hyphen-stripping is a deliberate simplification that can artificially inflate WER for contractions once real transcripts land (mirroring how the diacritic-preservation choice was already documented) — a real, if minor, "don't mistake this for a regression" landmine worth flagging before S2's data arrives; and hardened `reference.tsv` malformed-line handling to fail with the same actionable-guidance style used everywhere else in that file instead of a raw exception.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 203/203 (190 + 13 new WER-calculator tests), `Soneto.Platform.Windows.Tests` 97/97 + 2 pre-existing skips, `Soneto.Platform.Linux.Tests` 52/52 — all unchanged from item 11 plus the new additive tests. `Category=Corpus` exclusion from the default run reconfirmed working.
- **Phase 1's main build order (items 1-12) is now code-complete**, with two honestly-documented, structurally-blocked exceptions carried forward rather than papered over: item 11's Linux/Wayland real-hardware verification (needs spike S5, real Fedora hardware) and this item's corpus-regression assertion (needs spike S2, a real 60-file EN/RO corpus) — plus the still-open ⚠️ item 6/7 Windows hardware-verification re-check flagged during item 9's review. Item 10b (24-hour soak test) remains separately not-started.

- **Item 11 — Linux hotkey + injector, green (with an unusual, explicitly-flagged verification constraint).** Built via `Docs/soneto-implementation-plan-phase0-1.md` §1.9's spec — but this item is gated on spike S5 (Fedora KDE Wayland), which is `⬜ Not started` and needs real Linux hardware this agent session doesn't have. **The user was asked directly and chose: build best-effort code per the written spec now, with everything hardware-dependent honestly documented as unverified rather than fabricated** — same pattern this project already uses for items 4b/6/7/9/10's own "can't verify X from this session" gaps, just spanning a whole platform this time. `src/Soneto.Platform.Linux/`: `LinuxHotkeySource` (multi-keyboard `/dev/input/event*` enumeration + epoll-multiplexed single reader thread + inotify hotplug watch, mirroring `WindowsHotkeySource`'s "hook thread never does work, post-and-return" discipline), `LinuxTextInjector` (`wl-copy`/`wl-paste`/`xclip` clipboard via `XDG_SESSION_TYPE` backend detection, `ydotool key`/`ydotool type` for paste-chord/UnicodeSynth, a SHA-256 content-hash restore guard replacing Windows' OS sequence-number guard since Linux has no equivalent), plus `EvdevKeyMapper`/`KeyboardDeviceFilter`/`ClipboardHashGuard`/`ClipboardBackendSelector` as pure, unit-testable decision logic extracted from the actual syscalls/process calls (this project's established separation pattern). `scripts/setup-linux.sh` (input group, `/dev/uinput` udev rule, `ydotoold` systemd unit, red/green verification) written but never executed. **Deliberate, explained scope decision: `EVIOCGRAB`-based key suppression is NOT implemented** — the plan itself explicitly warns to test whether trigger-key leakage is tolerable before building the invasive whole-device grab path, and building it blind with no hardware to validate it doesn't break someone's keyboard was judged the wrong risk; `HotkeyBinding.Suppress` is accepted but logs a one-time warning that the trigger key will leak through, pending S5.
- **Independent verification found the `input_event`/`EVIOCGBIT`/`epoll_event` struct-layout assumptions are not just internally self-consistent but genuinely CORRECT against real Linux/glibc x86-64 ABI facts** (a stronger claim than the implementer made for themselves, who only claimed internal consistency) — worth recording as a case where the honest "I can't verify this on real hardware" framing turned out to undersell code that reasoning from known kernel/glibc facts could actually confirm.
- **Code review found no blocking issues but five real should-fix bugs, all fixed same day — the most significant being a ninth instance of this project's recurring "stale background execution touches superseded state" pattern.** (1) `LinuxHotkeySource.ReaderLoop` read shared mutable instance fields (`_epollFd`/`_channel`/etc.) live on every loop iteration instead of capturing them as immutable locals at thread-creation time, and `StopInternalAsync` discarded `Thread.Join`'s return value before closing fds — meaning a reader thread that failed to join within the 2s timeout (rare, but real: a stalled log sink, a bad GC pause) could keep running, loop around, and read a SUBSEQUENT `RestartAsync`'s new fds/channel, silently stealing epoll events from the real new reader or writing a phantom hotkey edge into the new session — the same shape of bug as `WindowsHotkeySource`'s heartbeat-timer race and `SessionController`'s `_maxDurationTimer` race, just via unguarded instance-field reads instead of a timer object. Fixed by capturing everything the reader thread needs as parameters at thread-creation time (never reading live instance state from inside the loop) and treating a `Join` timeout as Critical rather than silently proceeding to close fds out from under a possibly-still-alive thread. (2) `StartAsync` leaked already-opened device fds if a later step in the same startup sequence threw (no try/finally), compounded by the caller chain (`SessionController`, `Program.cs`) not disposing the hotkey source on this specific failure path either — fixed by making the class self-cleaning on its own partial-failure path. (3) The `Faulted` event's invocation was unprotected on one specific path (the reader thread's outer catch-all), unlike every other subscriber-invocation site in the same class — a throwing subscriber reached via that path would have crashed the whole process on a raw background thread, violating item 10's "daemon never exits" principle; fixed with the same try/catch discipline already used everywhere else. (4) The hotplug inotify watch fired a full restart on ANY `/dev/input` directory change, not just keyboard-relevant ones — fixed by parsing and filtering inotify event names to `event*` before treating a change as fault-worthy. (5) `RealProcessRunner.RunAsync` read stdout then stderr sequentially, a known `Process` deadlock pattern if a child process fills the unread stream's OS pipe buffer while blocked — fixed by reading both streams concurrently via `Task.WhenAll`. Also added 6 new tests for `LinuxTextInjector` (previously untested despite being the class that owns the restore-guard ordering code review had to trace by hand), including one proving the clipboard is read before any write during a restore attempt — closing the one real test-coverage gap flagged.
- `dotnet build soneto.slnx` — 0 warnings/errors, all TFMs/projects including the new `Soneto.Platform.Linux`/`Soneto.Platform.Linux.Tests`. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 190/190 (unchanged), `Soneto.Platform.Windows.Tests` 97/97+2 pre-existing skips (unchanged, confirming the `Program.cs` composition-root refactor didn't regress the Windows path), `Soneto.Platform.Linux.Tests` 52/52 (new).
- **Honest, explicit gap — this item's own "done when" criterion cannot be satisfied from this session:** "end-to-end dictation on Fedora KDE; multi-keyboard enumeration and hotplug verified by plugging in a second keyboard mid-session" needs real Linux/Wayland hardware. What's actually verified: every genuinely pure decision-logic piece (keyboard-vs-non-keyboard device filtering, evdev key-alias mapping, clipboard hash-guard sequencing, session-type backend selection, inotify event-name parsing) via real unit tests against fakes/synthetic data — 52 of them. What's compiled but never executed against a real kernel: every actual `open`/`ioctl`/`epoll_*`/`inotify_*` syscall, every actual `wl-copy`/`wl-paste`/`xclip`/`ydotool` process invocation, `scripts/setup-linux.sh`. Whether Right Ctrl (or any trigger) leaking is actually tolerable, whether `EVIOCGRAB` would be safe to add later, and real multi-keyboard hotplug all remain exactly what spike S5 exists to answer — S5 itself was not run and is not claimed to be resolved.

- **Item 10 — error handling + watchdog + recovery, green.** Reconciled a real discrepancy between plan §1.4's literal state table (`Recording | audio device lost → Faulted`, permanent) and §1.12's error-handling matrix (same scenario should be "auto" recoverable — abort, fall back to default device): `SessionController.FinalizeRecordingAsync`'s catch block now transitions to the normal `Cooldown→Idle` path instead of `Faulted`, relying on `CaptureDeviceResolver` (already built in item 4b) to auto-fall-back to the default device on the next key-down. Implemented the previously-entirely-unbuilt `UnicodeSynth` injection method in `WindowsTextInjector` (`SendInput` with `KEYEVENTF_UNICODE`, one INPUT pair per UTF-16 code unit, batched 50 per call with a 5ms gap between batches, per plan §1.8's exact fallback spec) — both as an explicit config choice and as an automatic fallback when the default `ClipboardPaste` method's clipboard-set step fails after all retries, closing §1.12's "clipboard set fails → fall back to UnicodeSynth, auto" row. Most of §1.12's matrix (model missing/hash-mismatch, hook-dies watchdog, decode-throws/timeout, config-invalid, audio-stream-open-failure logging, stream-always-closes-in-finally) was confirmed already correctly handled by items 2-9 — this item's real new work was these two gaps plus new daemon-level resilience tests proving all four of the item's literal done-when scenarios (kill the hook, unplug the mic mid-record, delete the model, corrupt the config) survive without crashing the daemon.
- **Two real issues found by code review, one of them a genuine clipboard-content-loss bug in the brand-new UnicodeSynth fallback path.** (1) **[Blocking]** The new automatic-fallback branch (clipboard-set fails after retries → fall back to UnicodeSynth) skipped straight to `SendUnicodeSynthAsync` without ever attempting to restore the pre-existing clipboard content it had backed up — but a failed clipboard "set" is NOT guaranteed to be a no-op: `WriteUnicodeTextToOpenClipboard` (item 7c) calls `EmptyClipboard()` (destroying the previous content) BEFORE `SetClipboardData()`, so a `SetClipboardData` failure after a successful `EmptyClipboard` — a real, already-documented scenario from item 7c's own history, not hypothetical — would silently and permanently lose the user's original clipboard content. Fixed by capturing a pre-set sequence number and attempting the same atomic, sequence-guarded restore used everywhere else in the class before falling back to UnicodeSynth, correctly still skipping the restore if a genuine user copy happened during the failed retry window. (2) **[Should-fix, honesty correction rather than a code bug]** Code review traced the real `PortAudioCapture`/`CaptureModeController` call chain and found the "audio device lost" catch block this item relies on for the Faulted→auto-recovery reconciliation is very likely unreachable against real hardware today — `PortAudioCapture.StopAsync()`'s only failure-prone call catches and only logs, never rethrows, so a physically-unplugged mic discovered at key-up time doesn't actually propagate an exception through this chain in production (not a regression — item 9's original `Faulted` path had the identical reachability gap). The `SessionController`-side state-machine logic itself is correct and verified (no busy-loop risk, no stale-state issue); what needed fixing was the doc comment overclaiming this path as a confirmed real-hardware mechanism. Corrected to honestly document it as a defensive backstop with real device-loss-signal wiring flagged as a candidate for a future item, not silently claimed as solved.
- `dotnet build soneto.slnx` — 0 warnings/errors, both `net10.0`/`net10.0-windows` TFMs (`WINDOWS` symbol confirmed present). `dotnet test soneto.slnx` — `Soneto.Core.Tests` 190/190 passed, `Soneto.Platform.Windows.Tests` 97/97 passed + 2 pre-existing unrelated skips.

- **Item 9 — SessionController, green. The milestone item: real end-to-end dictation wiring on Windows.** `src/Soneto.Core/SessionController.cs` implements plan §1.4's full state machine (`Initializing→Idle→Recording→Finalizing→Transcribing→Injecting→Cooldown→Idle`, plus `Faulted`) as a single-worker `Channel<SessionCommand>` consumer, wiring together every component built in items 3-8 (`IHotkeySource`, `CaptureModeController`, `SileroVadDetector`, `ITranscriber`, `PostProcessorChain`, `ITextInjector`) — platform-agnostic, in `Soneto.Core`, consuming only interfaces/already-built classes. `src/Soneto.Daemon/Program.cs` now composes the real Windows implementations (`WindowsHotkeySource`, `PortAudioCapture`, `SherpaOnnxTranscriber`, `WindowsTextInjector`) into a real running daemon, replacing the item-2 placeholder demo scope. All 16 table rows and all 6 numbered edge cases (§1.4) have real, assertion-backed tests (31 in `SessionControllerTests.cs`) verified independently by both a test-runner and a code-reviewer pass, not just claimed.
- **Two blocking issues found by code review, one of them serious enough to have made the entire item's real-world deliverable inert.** (1) The `#if WINDOWS` gating used for the new daemon wiring (following the exact pattern items 6/7 already used for `--watch-hotkey`/`--inject`) relied on a `WINDOWS` preprocessor symbol that **was never actually defined anywhere in the repo** — confirmed directly via `dotnet build -getProperty:DefineConstants`, which returned only `TRACE;DEBUG` even on the `net10.0-windows` TFM (the SDK generates `NET10_0_WINDOWS`, not a bare `WINDOWS`, and nothing defined the latter manually). Every `#if WINDOWS` block in `Program.cs` was silently dead code on every build configuration, including the Windows one — confirmed by inspecting the compiled DLLs' metadata directly (the Windows-only method names and log strings were provably absent from the binary pre-fix, present post-fix). **This means items 6/7's own `--watch-hotkey`/`--inject` commands were equally dead this whole time** — their previously-recorded "manually verified end-to-end against real Notepad" results in this changelog/PROJECT-MEMORY describe output that could only come from the real branch running, which was provably unreachable from the committed build config at review time. This is a real, unresolved gap: **items 6 and 7's manual hardware-verification claims should be treated as unconfirmed until someone re-runs them against the now-fixed build** — either an undocumented local override was used at the time (not reproducible from what's committed) or something in the build config changed since. Fixed here by properly defining `WINDOWS` for the `net10.0-windows` TFM in `Soneto.Daemon.csproj`, with the missing-symbol root cause and its blast radius documented directly in the csproj so it can't silently regress unnoticed again. (2) A genuine, if narrow, timer-disposal race matching this project's now-eight-times-recurring concurrency pattern: `SessionController.DisposeAsync()` called `CancelMaxDurationTimer()` *before* awaiting the worker task, meaning it could run concurrently with the worker thread if `DisposeAsync` (e.g. the daemon's real Ctrl+C/SIGTERM path) raced a real key-down/up — the same class of bug as item 4c's WarmIdle timer and item 6's heartbeat timer, this time with a self-consistent-looking-but-false doc-comment claim ("only ever touched by the worker thread") rather than a missing lock. Fixed by reordering the cancel call to run strictly after `await _workerTask`, eliminating the two-thread access entirely instead of adding a lock — not caught by either of the two dispose-race tests added in the prior round (both happened to call `DisposeAsync` outside the actual race window, which is only a handful of instructions wide and was found by tracing the code by hand, not by a test).
- Also worth recording as a genuinely positive result this time, not just bug-fixing: a prior test-runner verification flagged three coverage gaps (hook-fault arriving mid-Transcribing/mid-Injecting, `DisposeAsync` while genuinely `Recording`, VAD's non-discard path only ever exercised via the disabled-VAD bypass) and asked the implementer to add tests closing them *before* code review — all three tests were added, and **none of them found a bug**; the implementation held up under closer scrutiny for those three specific scenarios. Not every gap this project finds turns out to be a real defect — this is useful to remember alongside the eight-times-recurring pattern, so as not to over-correct into assuming every untested corner must be broken.
- `dotnet build soneto.slnx` — 0 warnings/errors, both `net10.0`/`net10.0-windows` TFMs. `dotnet test soneto.slnx` — `Soneto.Core.Tests` 187/187 passed, `Soneto.Platform.Windows.Tests` 85/85 passed + 2 pre-existing unrelated skips.
- **Honest gap, same category as items 4b/6/7's own documented gaps:** no agent session on this machine can produce real audio or safely drive unsupervised synthetic input against a live desktop (two prior recorded near-misses), so genuine human-in-the-loop end-to-end verification — press the real hotkey, speak into a real mic, release, watch punctuated text land in a real focused app — remains outstanding and is explicitly not claimed as done here.
- **Separately noted, not investigated in this item:** `Soneto.Platform.Windows.Tests`' `ClipboardManagerTests` (item 7c, real-OS-clipboard tests) showed intermittent failures on this machine during this session's test runs, a different specific test failing each time — looks environment-flaky rather than a regression from this item's changes, but not yet root-caused.

- **Item 8 — post-processor chain, green.** `src/Soneto.Core/PostProcessing/` adds the plan §1.7 four-stage stub chain: `UnicodeNormalizerProcessor` (order 10, NFC + cedilla `ş`/`ţ`→comma-below `ș`/`ț`, always on — the plan's prose overrides the existing-but-unused `PostProcessConfig.NormalizeUnicode` toggle, documented explicitly), `SpokenCommandsProcessor` (order 20, fixed EN/RO structural-command table — "new line"/"new paragraph"/"linie nouă"/"paragraf nou" → control characters), `WhitespaceCleanerProcessor` (order 30, horizontal-whitespace-only collapse, newline-preserving), `TrailingSpaceProcessor` (order 40), plus `PostProcessorChain` (sorts by `Order`, threads `PostProcessResult` stage-to-stage). All pure platform-agnostic string logic in `Soneto.Core`, no audio/model/hardware dependency.
- **Two real bugs found across two review round-trips, both in the "looks right, mangles a real case" family rather than concurrency this time.** (1) The first implementation's `SpokenCommandsProcessor` matched command phrases anywhere they appeared as free-standing words in an utterance — an independent test-runner verification caught that this directly violated the plan's own emphasized §1.13 requirement ("critically, a sentence containing the words in a non-command context isn't mangled"): `"my new line of business"` → `"my \n of business"`. Fixed by requiring punctuation/utterance-clause boundaries on both sides of the phrase (start/end of string, or `,.!?;:` with optional whitespace) — since Parakeet already emits real punctuation, this leverages the model's own signal to distinguish an intentional command ("okay, new paragraph, next item") from idiomatic prose containing the same words, at the honest cost of some legitimate mid-utterance commands spoken without a natural pause no longer firing (documented as a known, accepted tradeoff, not silently swept under the rug). (2) Code review then caught a genuine boundary bug in `WhitespaceCleanerProcessor`'s original stage ordering: the 3+-newline cap ran before the horizontal-whitespace trim, so whitespace-separated near-blank lines (e.g. `"\n \n \n"`) never became literally adjacent and were never capped to 2 — exactly the kind of off-by-one/ordering bug this project's history keeps finding at stage/component boundaries. Fixed by moving the trim before the cap, with a regression test locking in the fix.
- Two should-fix nits also applied: `TrailingSpaceProcessor`'s doc comment now accurately states its real rule ("ends in any non-whitespace character," not literally a word character — trailing punctuation counts too, deliberately, matching how Parakeet emits sentence-final punctuation) instead of just restating the plan's more ambiguous wording; a dead `using System.Text;` removed.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` (default filter) — 241 passed (156 `Soneto.Core.Tests` + 85 `Soneto.Platform.Windows.Tests`), 2 skipped (pre-existing, unrelated), 0 failed.

- **Item 7c — clipboard sequence guard + policy, green.** `Soneto.Platform.Windows/ClipboardManager.cs` replaces item 7's deliberately unconditional/unguarded restore with an atomic, sequence-guarded one, plus `clipboardPolicy`-driven non-text-format handling — the two pieces item 7's own doc comments explicitly deferred. `Save()` now enumerates clipboard formats via `EnumClipboardFormats` and flags non-text content using the S4 spike's validated "text family" allow-list (`CF_UNICODETEXT`/`CF_TEXT`/`CF_OEMTEXT`/`CF_LOCALE` — a naive "anything but `CF_UNICODETEXT`" check false-positives on Windows' own auto-synthesized `CF_LOCALE`/`CF_OEMTEXT` companions). New `RestoreUnicodeTextWithSequenceGuard`/`Async` open the clipboard once per attempt, check `GetClipboardSequenceNumber()` while still holding it open, and only write inside that same critical section — the exact atomic pattern S4's spike fixed a TOCTOU race with, ported faithfully rather than reintroduced. `WindowsTextInjector` now skips restoration entirely (log only, never overwrites) when `textOnly`/`bestEffort` policy and the original clipboard held non-text formats; `bestEffort`'s full OLE round-trip is deliberately not implemented in Phase 1, per the plan's own explicit call-out that it's not worth the complexity here. New `Soneto.Core.Abstractions.ClipboardPolicy` enum + `InjectionOptions.Policy` field plumb the config's policy through to the platform injector (previously collapsed into a single `RestoreClipboard` bool that couldn't distinguish `textOnly` from `bestEffort`).
- **Code review found a seventh instance of this project's recurring "looks atomic/correct but isn't" pattern, this time self-inflicted rather than a cross-thread race.** The atomic restore's retry loop called `EmptyClipboard()` before allocating/writing the replacement text — but `EmptyClipboard()` itself bumps `GetClipboardSequenceNumber()` regardless of whether the subsequent `SetClipboardData` ever succeeds. So a transient failure (e.g. `GlobalAlloc`/`SetClipboardData` briefly blocked by another clipboard manager — exactly the case the retry loop exists to absorb) would leave the sequence number already bumped by *our own* call; the next retry attempt would then see a "changed" sequence number and misreport a transient write failure as `SkippedSequenceChanged` ("user copied during window"), abandoning the restore with the clipboard left genuinely empty. Fixed by reordering `WriteUnicodeTextToOpenClipboard` so `EmptyClipboard()` runs only immediately before `SetClipboardData` (narrowing the self-perturbing window to one call), and by having the guarded-restore loop treat any failure *after* a successful `EmptyClipboard()` as immediately fatal (`Failed`, no further retries) rather than looping back into a sequence check it has already invalidated itself.
- New tests in `ClipboardManagerTests.cs` prove both done-when criteria with real clipboard mutations (not mocks): a genuine second clipboard write mid-restore-window correctly aborts the atomic restore with `SkippedSequenceChanged` and leaves the newer content untouched; a real synthetic `CF_BITMAP` on the clipboard is correctly flagged `HasNonTextFormats=true`, while a plain text-only clipboard correctly is not (proving the CF_LOCALE/CF_OEMTEXT allow-list fix holds). All new tests are pure clipboard-API calls on this process — no `SendInput`, no real-window interaction, consistent with this project's standing caution around live-desktop injection testing.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` (default filter) — 193 passed (85 + 108), 2 skipped (pre-existing, unrelated), 0 failed.

- **Item 6 — `IHotkeySource` Windows implementation, green.** `Soneto.Platform.Windows/WindowsHotkeySource.cs` promotes S3's spike into real product code via SharpHook 8.0.0, carrying forward both of S3's corrections: `VK_LCONTROL` (not generic `VK_CONTROL`) for reading held modifiers, and awareness of the block-callback silent-drop finding that motivates the heartbeat/watchdog design here. Callback-thread discipline (`OnKeyPressed`/`OnKeyReleased` doing only a comparison, a `Volatile.Write`, and a non-blocking channel write, with the actual public event-raising happening on a separate consumer task) was confirmed clean by two independent reviews — the best instance of this exact pattern across every item that's touched it so far.
- **Found a fifth instance of this project's recurring concurrency-bug pattern anyway — one level removed from the hook callback itself.** The heartbeat/fault-detection timer had a `Timer.Dispose()` race: an in-flight probe check (sleeping through a ~750ms wait window) could survive a concurrent `RestartAsync` call and then fire a spurious `Faulted` event against the brand-new, healthy hotkey source instance moments after a successful restart — the exact shape of bug that already hit item 4c's WarmIdle timer. Fixed with the same generation-token pattern that fixed that one, proven via a genuine concurrent race test (a sequential reflection-based test, item 4c's exact original technique, was correctly recognized as insufficient to prove this particular race, since the idle check reads live state a restart resets) confirmed to fail pre-fix and pass post-fix. Worth remembering: even the item specifically designed to avoid this pattern, with the pattern's own history in mind, still had it show up somewhere less obvious — don't stop scrutinizing background threads/timers just because the main callback looks clean.
- Also fixed on review: documented "must not block" contracts on the `Pressed`/`Released` events and on `StartAsync`/`RestartAsync`/`DisposeAsync`'s single-caller concurrency contract (both previously implicit); an honest "standing gap" doc note on the heartbeat probe's real side effect (it injects a genuine system-wide synthetic F24 keystroke roughly every 60-75s of hotkey inactivity — untested against real-world macro tools/RDP configurations, though F24 is a reasonable inert choice).
- **Confirmed environmental limitation for future test authoring, independently reproduced by two separate agents:** SharpHook/uiohook doesn't support two concurrent hook instances safely in one process on this machine (event delivery goes entirely to one instance, and stopping/awaiting hangs) — doesn't affect production (only one instance ever runs in the real daemon) but matters for how any future test against this class gets written.
- 146 tests pass (108 + 38, up from 108 pre-item-6), including real OS-level `EventSimulator`-driven end-to-end suppression and modifier-detection tests — genuine synthetic hardware input, not a fake.
- **Standing honest gaps, same shape as items 4b/4c's mic/cue gaps:** physical key + focused-GUI-app leak testing, physical Shift-held-during-a-real-trigger-press, 30-minute idle survival, and lock/unlock survival all need a human at a keyboard.

- **Item 7 — `ITextInjector` Windows (base injection), green.** `Soneto.Platform.Windows/WindowsTextInjector.cs` + `ClipboardManager.cs` promote S4's spike into product code, deliberately deferring the modifier sanitiser (item 7b) and the clipboard sequence-guard/policy hardening (item 7c) per this project's own incremental build order. Verified end-to-end against real Notepad via UI Automation: exact text match, correct diacritic byte encoding, ~203ms elapsed.
- **Found a genuinely deterministic bug, not a hypothetical or a race** — the sixth (and a distinctly new-shaped) instance of this project's pattern of concurrency/cross-component assumptions breaking in a first pass. Configuring `LeftControl` or `LeftShift` as the hotkey trigger — both explicitly supported — broke every single paste: the hotkey hook (item 6) observes ALL keyboard events system-wide including the injector's own synthetic `SendInput` calls, so the paste chord's own Ctrl/Shift-down got caught by the hook, suppressed, and turned into phantom hotkey events. 100% reproducible, not load-dependent, and easy to miss since the default `RightControl` trigger never collides with the paste chord's modifiers. Fixed with the technically correct mechanism — SharpHook's `IsEventSimulated` (backed by Windows' `LLKHF_INJECTED` flag) — rather than a workaround, at the honest cost of two hotkey tests that can no longer distinguish "test-simulated physical press" from "real synthetic input" (marked skipped with a clear explanation, not silently weakened or hidden).
- **Six should-fix items also found and fixed:** a missing app-switch diagnostic (the plan's own "log both handles" requirement, needed to tell when a user Alt-Tabbed away mid-dictation); cancellation mid-injection leaving the clipboard permanently clobbered with the transcript and no restore attempt; `ClipboardPolicy: "never"` being silently ignored even though honoring it needed none of the deferred 7c work; a blocking, non-cancellable `Thread.Sleep` in the async retry path; `SendInput` splitting the paste chord across multiple calls (reopening the exact keystroke-interleaving risk the API exists to prevent, on the synthetic side this time); and a hardware test that was genuinely flaky (2/3 pass rate independently observed) due to a fixed-delay guess, now 8/8 stable after switching to a polling readiness check.
- 163 tests pass + 2 documented skips (the honest tradeoff from the `IsEventSimulated` fix), 0 warnings/errors.

- **Item 7b — modifier sanitiser, green.** `Soneto.Platform.Windows/ModifierSanitizer.cs` suppresses physically-held Shift/Alt/Win/Left-Control before the paste chord and restores only what's still held afterward, excluding whichever one (if any) matches the configured hotkey trigger — same algorithm S4's spike already validated, now wired into `WindowsTextInjector.InjectAsync`.
- **Two independent reviews (test-runner + code-reviewer) converged on the same two real gaps.** (1) Left Control was excluded entirely from the sanitiser, but the paste chord always hardcodes a synthetic Left-Ctrl down/up — a physically-held Left Ctrl unrelated to the trigger would get its logical state desynced to "up" by the chord's own key-up, the same orphan-key-up problem the plan describes for Shift/Alt/Win. Fixed by adding `VK_LCONTROL` (left only, matching the chord's own hardcoded usage) to the sanitised set. (2) the sanitiser's own trigger-alias table didn't cover `HotkeyKeyMapper`'s fallback raw-`KeyCode`-name parsing path (e.g. `"VcLeftShift"` is a valid, accepted trigger the alias-only table would silently miss), reopening item 7's trigger-collision bug at the sanitiser layer for that class of config. Fixed by having `ResolveExcludedVk` delegate to `HotkeyKeyMapper.ToKeyCode` directly — one shared source of truth instead of two alias tables that could drift apart.
- Also fixed: `ModifierSanitizer` had no `InternalsVisibleTo` wiring, so the test-runner's 22-test pure-logic suite could only reach it via reflection; added the attribute to `Soneto.Platform.Windows.csproj` and simplified the tests to call it directly, plus two new cases covering the raw-`KeyCode`-fallback path.
- `dotnet build soneto.slnx` — 0 warnings/errors. `dotnet test soneto.slnx` (default filter) — 191 passed, 2 skipped (pre-existing, documented), 0 failed. Hardware-category tests deliberately not re-run this session (a hardware test's synthetic input briefly touched the real live desktop earlier in this session) — not needed here since only pure-logic paths changed.

## 2026-08-31

- **Item 5 — VAD integration, green.** `Soneto.Core/Asr/SileroVadDetector.cs` wraps sherpa-onnx's `VoiceActivityDetector`/`SileroVadModelConfig` (API shape confirmed by reflecting over the real installed package, since neither plan doc gives a VAD config example, the same discovery process item 3's ASR wiring needed) to trim leading/trailing silence from a 16kHz mono buffer per plan §1.5.
- **VAD model ships as a committed embedded resource, not a `ModelManager` download** — investigated and confirmed Silero's ~630KB ONNX model isn't bundled in the NuGet package (unlike the ASR model, ships compiled bindings only); fetched once from the same k2-fsa/sherpa-onnx release feed, SHA-256 independently verified, committed to source. Confirmed reasonable on review: the plan's "never commit to repo" language is stated specifically about the ~640MB ASR model's size, not as an absolute rule, and this codebase already has precedent (`warmup-en.wav`).
- **Code review found the whole-utterance discard check — the plan's own "main defence against decoding empty audio, which is where transducers hallucinate" — was structurally near-unreachable in the first implementation, not just set to a lower bar than the plan states.** It reused `VadConfig.MinSpeechMs` as both Silero's native per-segment filter AND the whole-utterance discard threshold; since total speech duration is by construction always ≥ any single segment that survived the per-segment filter using that same number, the discard check could essentially never fire once any segment was found at all. Both an independent test-runner and code-reviewer converged on this same finding and the same fix. Corrected with a genuinely separate config field, `VadConfig.MinUtteranceMs` (default 300, matching the plan's literal number) — proven with a test confirmed to fail against the pre-fix logic and pass against the fix, closing the previously-unreachable "real segments found, but combined span still under the discard floor" scenario.
- **New pattern worth remembering, distinct from the concurrency pattern tracked since item 4b:** reusing one config value for two conceptually different purposes can silently defeat a safety mechanism — it looked correct, built cleanly, and passed a manual demo; the bug only surfaced when a reviewer asked "can this check actually ever fire, given how the two numbers interact?"
- Also fixed on review: a cross-process race and weak length-only integrity check on the VAD model's extracted temp file (moved to a user-scoped cache directory with SHA-256 content validation and an atomic move, mirroring item 3's download-safety pattern) and a per-window allocation in the trim loop.
- CLI: `--record` now runs VAD trim on captured audio and reports boundaries before transcribing; new `--vad-demo <wav>` and `--vad-demo-transient` flags demonstrate boundary detection and the open-transient-discard case without needing live hardware or a real microphone. Verified independently with real numbers on real speech (`en-16k.wav`, correctly kept, only 70ms of leading silence trimmed from a 3.85s clip) and confirmed Silero genuinely does learned voice-activity detection, not simple energy thresholding — a first test-writing attempt using a synthetic sine tone as "speech" was correctly rejected by the model, requiring real speech excerpts instead.
- `dotnet build` clean (0 warnings/errors); default `dotnet test` — 108/108, all hardware/model-free (the embedded VAD model needs no download or live audio device, unlike the ASR model's `Category=Corpus` tests).
- Set up project tracking: this changelog, `Docs/PROJECT-MEMORY.md`, and status checkboxes in `Docs/soneto-implementation-plan-phase0-1.md`.
- Repo state at this point: `soneto.slnx` solution with a single empty `Soneto.Core` project (default template `Class1.cs`, no packages). No spikes, no tests, no other projects yet.
- Starting Phase 0, Spike S1 (ASR latency and correctness via sherpa-onnx).
- **S1 — Green.** Built `spikes/s1-asr/`: .NET 10 console app wrapping `org.k2fsa.sherpa.onnx` 1.13.5, confirmed the `OfflineRecognizerConfig` shape from the plan matches the real package exactly (no changes needed), downloaded the parakeet-tdt-0.6b-v3 int8 model (~465MB) to `models/`. Warm decode ~200-270ms on a 5s clip (bar: <400ms), cold load ~1.4-1.75s. Thread sweep found the knee at **4 threads**, not 8 as originally assumed — updated as the new default seed for §1.6. Long clips (60s/120s, synthetic) decode without truncation, RTF ~0.05-0.08.
- Verified independently by a test-runner agent on different hardware (all pass criteria reproduced) and reviewed by a code-reviewer agent, which flagged three real issues: unisolated warm-up timing (iteration 1 could conflate onnxruntime's first-call lazy-init cost into the reported "warm decode" number), missing `WAVE_FORMAT_EXTENSIBLE` WAV support (would break on Audacity/OBS-recorded files, relevant for S2), and unhandled RIFF chunk padding (would desync parsing on odd-sized metadata chunks). All three fixed and re-verified (clean rebuild, re-run against all test clips).
- Open follow-up before fully trusting long-utterance behavior: only validated on TTS-synthesized 60s/120s clips, not real recorded speech; and the thread-count knee should be re-checked on the actual target laptop CPU (measured on a Ryzen 7 7700X dev desktop).
- **S1b — Green.** Built `spikes/s1b-audio/`: PortAudioSharp2-based device-latency harness plus a windowed-sinc polyphase resampler with a swept-sine + time-segmented-RMS-FFT aliasing test. Real hardware measured (9 input devices including a paired Bluetooth headset). Default mic (MME) time-to-first-sample p95=58ms (bar: <150ms) — **`OnDemand` confirmed as the default capture mode.** Resampler suppression ~144dB on both 48kHz->16kHz and 44.1kHz->16kHz (bar: 40dB), plus a new output-length correctness check for both ratios.
- **Notable finding: the plan's "32-tap" resampler figure (§1.5) is wrong** and has been corrected in the plan doc itself — a 32-tap filter physically cannot produce the required 7.8kHz->8kHz transition band (basic Blackman-window transition-width math). Real requirement: ~1200-1300 taps per phase for a single-stage design, independently verified via the standard formula. Flagged for Phase 1 item 4 to either budget for the real tap count or evaluate a cascaded halfband decimation design instead.
- **Notable finding: this machine's Bluetooth headset fails outright under `OnDemand`** (`Pa_StartStream` errors every trial, no timing ever recorded), and the same physical mic delivers silent buffers via WASAPI/WDM-KS while working fine via MME/DirectSound — real, reproduced-independently evidence (not a hypothetical) for why `WarmIdle` needs to exist as a near-term fallback, not just a documented option.
- Verified independently (test-runner reproduced all measurements near-exactly on a second run and checked the tap-count math by hand; code-reviewer decompiled the vendor `PortAudioSharp2` DLL to confirm the hand-written P/Invoke declarations were correct, and hand-verified the FFT implementation). Two should-fix issues found and patched: a GC handle leak in the trial harness (`Close()` vs `Dispose()` — the pinned handles from `PortAudioSharp.Stream` are only freed by `Dispose()`), and a missing output-length/edge-taper check that the plan's own §1.13 testing section calls out explicitly. Both fixed and re-verified (build clean, sweep still passes at ~144dB, new length check passes).
- Open follow-up: WASAPI/WDM-KS silent-capture and Bluetooth start failures are documented but not root-caused; no external USB mic was available to test; the S2-corpus resampled-vs-native WER comparison is deferred until S2's corpus exists; the ~14ms zero-padded edge-taper in the resampler is documented as a known limitation but not yet validated against real VAD-trimmed audio.
- **S3 — automatable parts green, manual verification pending.** Built `spikes/s3-hotkey-win/`: SharpHook 8.0.0-based global hook on Right Ctrl, timestamp jitter measurement, GetAsyncKeyState-based modifier reading, and a deliberate callback-block demo. Timestamp jitter p50≈0.2-0.5ms/p95≈0.5-1.9ms (bar: <20ms) via SharpHook's own EventSimulator (real SendInput events). `listen` mode confirmed end-to-end cross-process.
- **Two findings that correct the plan's own design, both confirmed via 4 independent reproductions:** (1) the plan's §1.4 edge case 3 wording ("Windows unhooks you" when the callback blocks) is inaccurate — what actually happens is the hook stays alive and works normally afterward, but a key-up event that arrives mid-block is silently and permanently dropped, producing an orphan DOWN with no matching UP. A hook-liveness watchdog would miss this entirely; the existing `maxDurationMs` force-finalize timer on the DOWN event is confirmed necessary. (2) generic `VK_CONTROL` is ambiguous with the Right-Ctrl trigger itself — it always reads "held" during a trigger press regardless of whether the user is also holding a genuine Ctrl key elsewhere. §1.8's modifier sanitiser must read `VK_LCONTROL` specifically; demonstrated directly (VK_CONTROL=True, VK_LCONTROL=False during a trigger press).
- Also documented for Phase 1: `SelfTest.cs`'s zero-I/O callback is the pattern to copy for `IHotkeySource`; `ListenMode.cs`/`BlockDemo.cs`'s in-callback `Console.WriteLine` is a spike-only affordance that must not be carried into product code, per the plan's non-negotiable "hook callback never does work" rule.
- Verified independently (test-runner reproduced jitter/modifier/block-callback findings across 4 total runs; code-reviewer confirmed P/Invoke correctness, hook lifecycle/disposal, and SuppressEvent handling, but caught the VK_CONTROL ambiguity before it could carry forward as a bug). Both review findings fixed and re-verified.
- **What's left for S3 to be fully green:** six items need a human with real hardware and real GUI apps focused — no character/modifier leak into Notepad/VS Code/Chrome/Windows Terminal, 60s physical hold, physical Shift+trigger, observing a leaked orphan key-up in a real app, 30-minute idle survival, and lock/unlock cycle survival. Step-by-step script is in `spikes/s3-hotkey-win/README.md`. Also worth a retest on the actual target machine: whether a true full-unhook is ever observed (not seen in this dev environment).
- **S4 — core algorithm green, per-app matrix needs a clean manual re-run.** Built `spikes/s4-inject-win/`: full clipboard-paste injection per §1.8/§5.2 (save clipboard, modifier sanitizer reusing S3's VK_LCONTROL fix, retry-on-collision SetClipboardData, SendInput paste, sequence-number-guarded restore). Diacritics confirmed correct at the byte level (U+0219/U+021B comma-below, not cedilla). All three required adversarial cases pass with real evidence: held-Shift correctly suppressed with no stuck modifier; the sequence-number guard correctly protects a user's fresh copy during the restore window; non-text clipboard content correctly skips restoration rather than being destroyed.
- **Two real bugs found and fixed in the safety-critical clipboard-restore path:** (1) restore failures were silently swallowed and misreported as success — fixed with a new `RestoreFailed` outcome; (2) a genuine TOCTOU race existed between the sequence-number check and the actual restore write (traced back to a gap in the plan's own §1.8 pseudocode, not something the spike introduced) — fixed with a real atomic open/check/write/close implementation. Carry the atomic pattern into Phase 1, not the plan's literal pseudocode.
- **Important operational finding, not a code bug:** while testing the per-app launch matrix, discovered the automated launcher trusts `GetForegroundWindow()` without verifying it belongs to the just-launched app — on this real, in-use desktop (with an actual signed-in Teams work account and Outlook/Word open), this produced false-positive "Injected" results where the paste actually landed on a stale leftover window. Caught by inspecting screenshots, not by trusting return values. Verified no real content was affected (no lingering drafts, nothing overwritten or sent) and cleaned up the stray test window. Only Notepad and Chrome-textarea are confirmed from that run; the remaining apps (VS Code, Chrome address bar, Windows Terminal, Teams, Outlook, Word) need a human-supervised one-at-a-time re-test via `countdown` mode, documented as a manual test script in the README. This is now a standing caution for any future automated multi-app testing on a live desktop.
- Also fixed: a `GlobalAlloc` memory leak on clipboard-write failure paths, undisposed `Process` handles, and a ~30% flake in the shift-hold adversarial test's read-back (root-caused to an unguarded UI-Automation timing race in the test harness, not the sanitizer — the sanitizer's own behavior was correct on every run including the "failing" ones). Verified 12/12 clean after the fix.
- Verified independently (test-runner reproduced all core-algorithm results, independently confirmed diacritic codepoints via a standalone script, and gave a fair assessment that the latency "miss" is legitimately explained rather than spin; code-reviewer found both blocking clipboard-safety bugs and confirmed the foreground-detection bug's plausibility from the code without re-executing the risky matrix).
- **Decision: S2 (Romanian accuracy) deferred, not run.** User has decided to assume Romanian accuracy is acceptable for now and prioritize English — a deliberate scope call, not a data-backed gate pass. Doesn't block Phase 1 (ASR layer is language-agnostic); does block the language-profile work in the build plan (§7.2) until actually run. Proceeding to Phase 1 per user direction.

## Phase 1 — Headless Daemon

- **Item 1 — Solution scaffold, green.** First real product code (not spike code). Built the full §1.2 layout: `src/Soneto.Core` (all five §1.3 abstraction interfaces — `IAudioCapture`, `ITranscriber`, `IHotkeySource`, `ITextInjector`, `IPostProcessor` — copied faithfully, no drift, verified member-for-member against the spec), `src/Soneto.Platform.Windows`, `src/Soneto.Platform.Linux`, `src/Soneto.Daemon` (minimal host, logs start/stop, no real logic yet), `tests/Soneto.Core.Tests` (2 placeholder tests), `tests/Soneto.Corpus/README.md`. Package versions carried over from the spikes that already validated them (sherpa-onnx 1.13.5, PortAudioSharp2 1.0.6, SharpHook 8.0.0) rather than guessing new ones.
- **Platform-isolation rule confirmed held:** `Soneto.Core` has zero platform-specific references or APIs — independently verified.
- **Two review fixes applied:** removed an unrequested `InvariantGlobalization=true` from all four new projects (would have broken Romanian diacritic-aware string casing/comparison — a real risk for this app's whole premise, caught before any code depended on it) — notably the implementer also caught and fixed it in `Soneto.Daemon.csproj` beyond the 3 files flagged in review, correctly reasoning that's the one that actually matters at runtime since it's the only executable project. Also removed a redundant `System.Text.Json` PackageReference that was producing an NU1510 warning on every build (the type ships in .NET 10's shared framework already).
- `Soneto.Daemon` multi-targets `net10.0;net10.0-windows` with a conditional `ProjectReference` to `Soneto.Platform.Windows` only under the Windows TFM — confirmed correct and working, but `dotnet run` on this project always needs an explicit `-f` flag (a real, easy-to-hit first-day friction point, now documented).
- Verified independently (test-runner confirmed build/test clean, zero architectural drift on both load-bearing constraints — platform isolation and interface fidelity — and caught the `dotnet run -f` gotcha by actually testing it; code-reviewer caught the `InvariantGlobalization` risk and the NU1510 warning, and confirmed everything else — nullable annotations, the `AppliedRule` doc comment the plan explicitly asks to preserve, judgment calls on underspecified types — is sound).
- **CI not set up:** this workspace has no git repository yet, so there's nowhere to attach it. Local `dotnet build`/`dotnet test` (both clean) stand in for now.
- Solution file uses the newer `.slnx` XML format rather than `.sln` as the plan's text literally says — a deliberate, better choice, noted so it doesn't read as an oversight against the spec.
- **Item 2 — Config load/save/hot-reload + logging host, green.** `Soneto.Core/Configuration/` (config model matching §1.10's schema field-for-field, path resolution, hot-reload via a debounced `FileSystemWatcher`, invalid-JSON/unknown-value fallback behavior) + Serilog logging wired into `Soneto.Daemon` (console + daily-rolling file sink, 7-day retention, `SessionId` enrichment plumbing, `--verbose` flag). `numThreads` default deliberately set to 4 (S1's actual finding) instead of the plan's literal 8, documented in-code and in memory.
- **Two blocking bugs found and fixed:** (1) a dispose/timer race in the hot-reload debounce — an in-flight `FileSystemWatcher` callback could create a fresh timer that survives past `Dispose()` and fires against an already-disposed service; (2) `LoadAsync`'s explicit "never throws" contract was actually violated for realistic first-run permission/AV failures (`UnauthorizedAccessException` wasn't caught), which could have crashed the whole daemon on first launch — exactly the failure mode §1.12 exists to prevent. Both fixed, plus a defensive top-level catch added in `Program.cs` as a last line of defense.
- **Six should-fix items also applied:** a misleading "Config hot-reloaded" log line that fired on every normal startup (not just real reloads); the config's own `logging.level`/`retainDays` fields were loaded but silently ignored — now wired in properly, with `level` truly live-reloadable; enum values were serializing as PascalCase instead of matching the plan's documented lowercase/camelCase schema examples — fixed to camelCase everywhere including the hand-written `captureMode` converter; documented (not changed) the intentional asymmetry where only `captureMode` gets a soft per-field fallback while other invalid enum values revert the whole config file, per what §1.13 actually specifies; strengthened the hot-reload test to actually prove rapid-fire writes collapse into a single reload rather than firing multiple times; added a `FileSystemWatcher.Error` handler so a buffer-overflow silently breaking hot-reload no longer goes unlogged.
- Verified independently (test-runner injected deliberate bugs into each of the 4 original config tests to confirm they're genuinely load-bearing, not vacuous, then reverted and reconfirmed a clean pass; also manually exercised hot-reload, JSON corruption, and unknown-value fallback against a running daemon and confirmed matching log output and file-sink behavior. Code-reviewer found the two blocking issues plus all six should-fix items via careful reading of the threading/exception-handling code, not just a black-box pass).
- 7/7 tests pass, 0 warnings, 0 errors across the whole solution after fixes.
- **Item 3 — `ITranscriber` + sherpa-onnx implementation + model manager, green.** S1's spike promoted to real product code. `Soneto.Core/Asr/SherpaOnnxTranscriber.cs` implements `ITranscriber` with the confirmed API shape from S1, warm-up against a bundled real-speech clip (fails loud at startup if empty, per §1.6). `Soneto.Core/Asr/ModelManager.cs` + downloader/extractor implement §1.6's full resolution order: config override → standard OS path → resumable HTTP download (genuine Range-request resume, independently verified) → SHA-256 verify-before-extract → hash-mismatch retry-once-then-fail → post-extraction file-presence check. Never runs inference against unverified weights — this ordering was traced through the code and confirmed correct by two independent reviewers. `Soneto.Core/Wav/WavReader.cs` ported from the S1 spike with both its review-fixed bugs (WAVE_FORMAT_EXTENSIBLE, RIFF padding) intact, now with real unit tests instead of spike-level trust. `--transcribe <wav>` CLI added to the daemon, verified end-to-end against the real 640MB model: cold load ~1.7-1.8s, warm-up ~70ms, decode ~190ms for a 3.8s clip, RTF ~0.049, correct punctuated output.
- **One blocking bug found and fixed:** `DisposeAsync` didn't synchronize with the decode semaphore, so a concurrent decode could race a dispose of the native ONNX recognizer — a genuine use-after-free hazard against native memory, not just a managed exception. Fixed by having dispose acquire the same gate a decode holds.
- **Five should-fix items also applied:** a stdout pipe-deadlock risk in the tar-extraction subprocess call; `tar`-not-found threw a raw unhandled `Win32Exception` instead of the clear error the code appeared to guard against (confirmed by direct reproduction); no stall protection on the model download (a hung connection would wait forever); a shared temp download filename that could collide between concurrent invocations; and a dev-only model-discovery convenience helper that walked up from the working directory with no check it was actually inside the Soneto repo, tightened with the same repo-identity marker the corpus test already used — flagged specifically because it bypasses hash verification by design and needed hardening before item 9 could plausibly reuse the pattern for real daemon startup.
- Verified independently (test-runner reproduced the "25/25 tests pass with the model directory renamed away" claim exactly, ran the real corpus test and `--transcribe` demo against the actual model, and directly reproduced the `tar`-not-found gap by pointing at a nonexistent executable; code-reviewer traced the full download/verify/extract code path by hand to confirm the safety-critical ordering, and found the dispose race by reading the semaphore usage carefully rather than via a black-box test).
- Default test suite (25 tests) confirmed passing with no model file present; 1 `Category=Corpus` test passes against the real model. 0 warnings, 0 errors across the whole solution after fixes.
- **Item 3b — Stream lifecycle + native memory, green.** Audited `SherpaOnnxTranscriber` for native `OfflineStream` disposal per §1.13's stress-test requirement — found it was already correct from item 3 (every `CreateStream()` call properly `using`-scoped, no fix needed, confirmed by direct code read rather than trusting the claim). Added a 1000-iteration real-decode stress test (`Category=Corpus`, excluded from the default run) asserting working set stays flat within 5% from iteration 50 onward, per the plan's literal criterion. Result: no leak, max deviation 0.11-0.18% across two independent runs (far inside the 5% bar).
- **Methodology independently scrutinized, not just trusted:** verified the test's detection floor (~45KB/decode given the baseline and tolerance) is well below the size of the realistic defect it targets (a missing `using` would leak a full per-utterance audio buffer, comfortably over 200KB, and would fail by roughly iteration 225 — not hide in the noise), while honestly documenting that a much smaller leak could in principle go undetected within 1000 iterations. Two minor fixes applied directly: a doc-comment caveat on the `WorkingSet64` measurement's limitations, and a `Process` handle leak in the test's own sampling helper (ironic for a leak-detection test, now fixed).
- Default test suite remains 25/25 green with the new stress test correctly excluded; 0 warnings, 0 errors.
- **Item 4 — Polyphase resampler, green.** `Soneto.Core/Audio/PolyphaseResampler.cs`, built as a **stateful streaming resampler**, not a straight port of S1b's whole-buffer spike design. §1.5's "accumulate into a growable list" wording implies the real pipeline calls the resampler once per ~512-frame audio buffer throughout a dictation — a stateless design would have repeated the spike's ~14ms zero-padded edge-taper artifact at every buffer boundary (roughly every 10ms) for the whole utterance, not just once. The stateful design maintains filter-history state across calls so zero-padding only happens at the true start/end of a stream. This resolves S1b's previously-unvalidated edge-taper limitation, not just documents it.
- Both resampling ratios (48kHz→16kHz exact 3:1, 44.1kHz→16kHz awkward 147:160) re-verified at ~144dB alias suppression, matching S1b's spike numbers, with the corrected tap counts (1321/phase and 1213/phase respectively) derived at construction time, not hardcoded.
- **The core streaming claim — bit-identical output between chunked and one-shot processing — was scrutinized hard, not just trusted**, since it's an unusually strong claim for floating-point DSP and the most likely place for a "looks right but isn't" bug to hide (e.g., a resampler that secretly buffers everything and only really processes at `Flush()`, which would trivially pass a chunked-vs-one-shot comparison without solving the actual real-time streaming problem). Independently verified via reflection-based scratch tests that ~99% of output is produced incrementally per-call (not deferred to `Flush()`), and that the internal history buffer stays correctly bounded even under a pathological 50,000-sample stream fed 1-2 samples at a time.
- Also verified: length correctness across odd chunk sizes, a mathematically-known reference-signal check (pure 1kHz tone, amplitude and frequency both independently confirmed), and `Flush()`/`Reset()` reuse semantics.
- **Three should-fix items from code review, all applied:** a stale transition-width calculation that could under-provision filter taps in the (currently unused) upsampling case — fixed with a more physically-correct formula than the one originally proposed, after the implementer traced through the math and found the literal suggested fix would have been wrong in the general case; per-call heap allocations in what becomes a real-time audio capture path, addressed with pre-sizing and a new buffer-writing overload; and a missing thread-safety doc note.
- Verified independently at every stage (test-runner wrote genuine scratch tests via reflection to rule out the "buffers everything until Flush()" shortcut and stress-tested pathological tiny chunk sizes; code-reviewer traced the buffer-trimming invariant by hand and confirmed it provably never discards data a future call needs).
- 51/51 tests pass (up from 25 pre-item-4), 0 warnings, 0 errors.
- **Item 4b — `IAudioCapture` (PortAudio), on-demand, green — with a real architectural correction, not just should-fix polish.** `Soneto.Core/Audio/PortAudioCapture.cs`: resolves the device fresh at start, probes 16kHz-direct support, picks the right path (direct or resample-via-item-4's resampler), logs the negotiated rate. First-pass review found this violated §1.4's explicit "audio callback writes into the ring buffer only, lock-free single-producer/single-consumer" requirement — the callback took a lock and did the ~1300-tap resampler convolution directly on the real-time thread, with a new benchmark measuring tail-latency spikes up to 40% of the buffer budget in a path this dev machine's own default device never exercises (it takes the easy direct-16kHz path).
- **Redesigned to a genuine lock-free ring buffer** (`SpscFloatRingBuffer.cs`, monotonic-counter design that cleanly sidesteps the classic full/empty ambiguity) with a dedicated consumer thread doing all resampling/RMS/buffer work — the PortAudio callback now does only a native-to-scratch copy, a bulk write into the ring buffer, and a signal. Also restored "first non-zero buffer" as the time-to-first-sample metric — a prior simplification to "first buffer arrival" would have masked this exact machine's own known WASAPI-silent-buffer failure mode (found in S1b) by reporting a fast, healthy-looking number for a device delivering pure silence.
- **A second review round scrutinized the redesign itself for classic lock-free bugs** (memory-ordering correctness, wraparound off-by-ones) and found none, backed by a new genuine multi-threaded stress test (a real producer racing a real consumer across 1.28M samples, plus deliberate backpressure/drop scenarios) — notably, the first draft of that test caught a real subtlety mid-authoring (an unthrottled producer can legitimately outrun even a keeping-up consumer at startup) and was fixed by correcting the test setup, not by loosening the assertion.
- Two small remaining should-fix items (a silent frame-truncation path with no diagnostic counter, non-volatile cross-thread read on `IsRunning`) applied directly. `--record 5` verified end-to-end against real hardware: device opens, real PortAudio callbacks fire, data flows correctly through the lock-free path at ~50ms latency (matching S1b's spike finding).
- **Standing honest gap, not a defect:** no agent session on this machine can physically produce sound near the microphone, so every verification independently got "pipeline works, but every buffer is silent" — the mechanics are genuinely proven on real hardware, but actual non-silent speech capture through this pipeline has not been. Flagged clearly for a human to close by running `--record` while speaking.
- 70/70 tests pass (up from 51 pre-item-4b), 0 warnings, 0 errors.
- **Item 4c — Capture modes + ready cue, green — two more blocking concurrency issues found and fixed.** `CaptureModeController.cs` formalizes the `OnDemand`/`WarmIdle`/`AlwaysOn` orchestration item 4b's demo did manually; `PreRollRingBuffer.cs` gives `WarmIdle` its full pre-roll from the 2nd utterance of a burst onward (verified via exact sample-count math — a 500ms pre-roll config produced an exact 8000-sample/500ms difference between utterance 1 and 2 of a burst, not just a log line); `AudioCuePlayer.cs` plays 880Hz ready / 220Hz failure tones through a genuinely separate output stream.
- **Two independent reviews converged on matching blocking findings.** (1) A real TOCTOU race in the `WarmIdle` idle-close timer — `Timer.Dispose()` doesn't guarantee an already-dequeued callback won't still fire, so a stale timer fire could silently kill a stream mid-recording of a brand-new utterance, producing zero/truncated audio indistinguishable from an ASR bug. This is the **fourth** time this project has caught a concurrency-discipline gap in a first implementation pass (after S3's hook callback, S4's clipboard atomicity, item 4b's audio callback) — recorded as a standing pattern in project memory to watch for going forward, since none of these were caught by manual testing, only by review. (2) `ReadyCue.None` was incorrectly silencing the failure tone too, contradicting the plan's explicit "silence is the worst possible feedback" reasoning for a failed stream-open specifically — a user opting out of the routine success beep would have gotten zero feedback while talking into a dead mic, exactly the worst case the plan calls out.
- **Fix rigor worth noting:** the timer race fix (a generation-token guard) was proven with a deterministic test that was confirmed to fail against the pre-fix code and pass against the fix — after first discovering that a pure wall-clock race test couldn't reliably distinguish "the timer legitimately won the race" from the actual bug. The failure-cue fix also surfaced and fixed a second real defect at its only call site (`Program.cs`'s CLI demo was nulling out the cue player entirely under `ReadyCue.None`, which would have silently defeated the whole fix in the one place it mattered).
- Also applied: a permanent two-resampler consumer-thread throughput test (tracking real but not huge margin — 61-68% of budget — on a code path still untested by any hardware in this environment, since the default device skips resampling entirely); a `CancellationToken` that was checked only at entry, not honored during the actual wait; and fire-and-forget error handling.
- 101/101 tests pass (up from 70 pre-item-4c), 0 warnings, 0 errors.
- **Standing honest gap, carried from item 4b:** ready/failure cue audibility and genuine non-silent speech capture through `WarmIdle`/pre-roll remain unverified by any agent session, since none can physically produce sound near a microphone or confirm a tone is audible through speakers. A human should verify both before fully trusting this path.
